// t3d-atmosphere
!function(t,e){"object"==typeof exports&&"undefined"!=typeof module?e(exports,require("t3d"),require("t3d-effect-composer")):"function"==typeof define&&define.amd?define(["exports","t3d","t3d-effect-composer"],e):e((t="undefined"!=typeof globalThis?globalThis:t||self).t3d=t.t3d||{},t.t3d,t.t3d)}(this,function(t,e,n){"use strict";const r="\n#define assert(x)\n\n#define Length float\n#define Wavelength float\n#define Angle float\n#define SolidAngle float\n#define Power float\n#define LuminousPower float\n\n#define Number float\n#define InverseLength float\n#define Area float\n#define Volume float\n#define NumberDensity float\n#define Irradiance float\n#define Radiance float\n#define SpectralPower float\n#define SpectralIrradiance float\n#define SpectralRadiance float\n#define SpectralRadianceDensity float\n#define ScatteringCoefficient float\n#define InverseSolidAngle float\n#define LuminousIntensity float\n#define Luminance float\n#define Illuminance float\n\n// A generic function from Wavelength to some other type.\n#define AbstractSpectrum vec3\n// A function from Wavelength to Number.\n#define DimensionlessSpectrum vec3\n// A function from Wavelength to SpectralPower.\n#define PowerSpectrum vec3\n// A function from Wavelength to SpectralIrradiance.\n#define IrradianceSpectrum vec3\n// A function from Wavelength to SpectralRadiance.\n#define RadianceSpectrum vec3\n// A function from Wavelength to SpectralRadianceDensity.\n#define RadianceDensitySpectrum vec3\n// A function from Wavelength to ScatteringCoefficient.\n#define ScatteringSpectrum vec3\n\n// A position in 3D (3 length values).\n#define Position vec3\n// A unit direction vector in 3D (3 unit-less values).\n#define Direction vec3\n// A vector of 3 luminance values.\n#define Luminance3 vec3\n// A vector of 3 illuminance values.\n#define Illuminance3 vec3\n\n#define TransmittanceTexture sampler2D\n#define AbstractScatteringTexture sampler3D\n#define ReducedScatteringTexture sampler3D\n#define ScatteringTexture sampler3D\n#define ScatteringDensityTexture sampler3D\n#define IrradianceTexture sampler2D\n\nconst Length m = 1.0;\nconst Wavelength nm = 1.0;\nconst Angle rad = 1.0;\nconst SolidAngle sr = 1.0;\nconst Power watt = 1.0;\nconst LuminousPower lm = 1.0;\n\n#if !defined(PI)\nconst float PI = 3.14159265358979323846;\n#endif // !defined(PI)\n\nconst Length km = 1000.0 * m;\nconst Area m2 = m * m;\nconst Volume m3 = m * m * m;\nconst Angle pi = PI * rad;\nconst Angle deg = pi / 180.0;\nconst Irradiance watt_per_square_meter = watt / m2;\nconst Radiance watt_per_square_meter_per_sr = watt / (m2 * sr);\nconst SpectralIrradiance watt_per_square_meter_per_nm = watt / (m2 * nm);\nconst SpectralRadiance watt_per_square_meter_per_sr_per_nm = watt / (m2 * sr * nm);\nconst SpectralRadianceDensity watt_per_cubic_meter_per_sr_per_nm = watt / (m3 * sr * nm);\nconst LuminousIntensity cd = lm / sr;\nconst LuminousIntensity kcd = 1000.0 * cd;\nconst Luminance cd_per_square_meter = cd / m2;\nconst Luminance kcd_per_square_meter = kcd / m2;\n\nstruct DensityProfileLayer {\n\tLength width;\n\tNumber exp_term;\n\tInverseLength exp_scale;\n\tInverseLength linear_term;\n\tNumber constant_term;\n};\n\nstruct DensityProfile {\n\tDensityProfileLayer layers[2];\n};\n\nstruct AtmosphereParameters {\n\tIrradianceSpectrum solar_irradiance;\n\tAngle sun_angular_radius;\n\tLength bottom_radius;\n\tLength top_radius;\n\tDensityProfile rayleigh_density;\n\tScatteringSpectrum rayleigh_scattering;\n\tDensityProfile mie_density;\n\tScatteringSpectrum mie_scattering;\n\tScatteringSpectrum mie_extinction;\n\tNumber mie_phase_function_g;\n\tDensityProfile absorption_density;\n\tScatteringSpectrum absorption_extinction;\n\tDimensionlessSpectrum ground_albedo;\n\tNumber mu_s_min;\n};\n",a="\nNumber ClampCosine(const Number mu) {\n\treturn clamp(mu, Number(-1.0), Number(1.0));\n}\n\nLength ClampDistance(const Length d) {\n\treturn max(d, 0.0 * m);\n}\n\nLength ClampRadius(const AtmosphereParameters atmosphere, const Length r) {\n\treturn clamp(r, atmosphere.bottom_radius, atmosphere.top_radius);\n}\n\nLength SafeSqrt(const Area a) {\n\treturn sqrt(max(a, 0.0 * m2));\n}\n\nLength DistanceToTopAtmosphereBoundary(const AtmosphereParameters atmosphere,\n\tconst Length r, const Number mu) {\n\tassert(r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tArea discriminant = r * r * (mu * mu - 1.0) +\n\t\tatmosphere.top_radius * atmosphere.top_radius;\n\treturn ClampDistance(-r * mu + SafeSqrt(discriminant));\n}\n\nLength DistanceToBottomAtmosphereBoundary(const AtmosphereParameters atmosphere,\n\tconst Length r, const Number mu) {\n\tassert(r >= atmosphere.bottom_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tArea discriminant = r * r * (mu * mu - 1.0) +\n\t\tatmosphere.bottom_radius * atmosphere.bottom_radius;\n\treturn ClampDistance(-r * mu - SafeSqrt(discriminant));\n}\n\nbool RayIntersectsGround(const AtmosphereParameters atmosphere,\n\tconst Length r, const Number mu) {\n\treturn mu < 0.0 && r * r * (mu * mu - 1.0) +\n\t\tatmosphere.bottom_radius * atmosphere.bottom_radius >= 0.0 * m2;\n}\n\nNumber GetTextureCoordFromUnitRange(const Number x, const int texture_size) {\n\treturn 0.5 / Number(texture_size) + x * (1.0 - 1.0 / Number(texture_size));\n}\n\n#if TRANSMITTANCE_MAPPING == 0\n\tvec2 GetTransmittanceTextureUvFromRMu(const AtmosphereParameters atmosphere,\n\t\tconst Length r, const Number mu) {\n\t\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\t\t\tassert(mu >= -1.0 && mu <= 1.0);\n\t\tfloat u = (mu + 0.15) / (1.0 + 0.15);\n\t\tfloat v = (r - atmosphere.bottom_radius) / (atmosphere.top_radius - atmosphere.bottom_radius);\n\t\treturn vec2(u, v);\n\t}\n#elif TRANSMITTANCE_MAPPING == 1\n\tvec2 GetTransmittanceTextureUvFromRMu(const AtmosphereParameters atmosphere,\n\t\tconst Length r, const Number mu) {\n\t\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\t\t\tassert(mu >= -1.0 && mu <= 1.0);\n\t\tfloat u = atan((mu + 0.15) / (1.0 + 0.15) * tan(1.5)) / 1.5;\n\t\tfloat v = sqrt((r - atmosphere.bottom_radius) / (atmosphere.top_radius - atmosphere.bottom_radius));\n\t\treturn vec2(u, v);\n\t}\n#else\n\tvec2 GetTransmittanceTextureUvFromRMu(const AtmosphereParameters atmosphere,\n\t\tconst Length r, const Number mu) {\n\t\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\t\t\tassert(mu >= -1.0 && mu <= 1.0);\n\t\t// Distance to top atmosphere boundary for a horizontal ray at ground level.\n\t\tLength H = sqrt(atmosphere.top_radius * atmosphere.top_radius - \n\t\t\tatmosphere.bottom_radius * atmosphere.bottom_radius);\n\t\t// Distance to the horizon.\n\t\tLength rho =\n\t\t\tSafeSqrt(r * r - atmosphere.bottom_radius * atmosphere.bottom_radius);\n\t\t// Distance to the top atmosphere boundary for the ray (r,mu), and its minimum\n\t\t\t// and maximum values over all mu - obtained for (r,1) and (r,mu_horizon).\n\t\tLength d = DistanceToTopAtmosphereBoundary(atmosphere, r, mu);\n\t\tLength d_min = atmosphere.top_radius - r;\n\t\tLength d_max = rho + H;\n\t\tNumber x_mu = (d - d_min) / (d_max - d_min);\n\t\tNumber x_r = rho / H;\n\t\treturn vec2(GetTextureCoordFromUnitRange(x_mu, TRANSMITTANCE_TEXTURE_WIDTH),\n\t\t\t\t\tGetTextureCoordFromUnitRange(x_r, TRANSMITTANCE_TEXTURE_HEIGHT));\n\t}\n#endif\n\nDimensionlessSpectrum GetTransmittanceToTopAtmosphereBoundary(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tvec2 uv = GetTransmittanceTextureUvFromRMu(atmosphere, r, mu);\n\treturn DimensionlessSpectrum(texture(transmittance_texture, uv).rgb);\n}\n\nvec3 GetTransmittance(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu, const Length d, \n\tconst bool ray_r_mu_intersects_ground) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tassert(d >= 0.0 * m);\n\n\tLength r_d = ClampRadius(atmosphere, sqrt(d * d + 2.0 * r * mu * d + r * r));\n\tNumber mu_d = ClampCosine((r * mu + d) / r_d);\n\n\tif (ray_r_mu_intersects_ground) {\n\t\treturn min(\n\t\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, transmittance_texture, r_d, -mu_d) /\n\t\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, transmittance_texture, r, -mu),\n\t\t\tDimensionlessSpectrum(1.0));\n\t} else {\n\t\treturn min(\n\t\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, transmittance_texture, r, mu) /\n\t\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, transmittance_texture, r_d, mu_d),\n\t\t\tDimensionlessSpectrum(1.0));\n\t}\n}\n\nDimensionlessSpectrum GetTransmittanceToSun(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu_s) {\n\tNumber sin_theta_h = atmosphere.bottom_radius / r;\n\tNumber cos_theta_h = -sqrt(max(1.0 - sin_theta_h * sin_theta_h, 0.0));\n\treturn GetTransmittanceToTopAtmosphereBoundary(\n\t\tatmosphere, transmittance_texture, r, mu_s) *\n\t\tsmoothstep(-sin_theta_h * atmosphere.sun_angular_radius / rad, \n\t\t\t\t\tsin_theta_h * atmosphere.sun_angular_radius / rad,\n\t\t\t\t\tmu_s - cos_theta_h);\n}\n\nInverseSolidAngle RayleighPhaseFunction(const Number nu) {\n\tInverseSolidAngle k = 3.0 / (16.0 * PI * sr);\n\treturn k * (1.0 + nu * nu);\n}\n\nInverseSolidAngle MiePhaseFunction(const Number g, const Number nu) {\n\tInverseSolidAngle k = 3.0 / (8.0 * PI * sr) * (1.0 - g * g) / (2.0 + g * g);\n\treturn k * (1.0 + nu * nu) / pow(1.0 + g * g - 2.0 * g * nu, 1.5);\n}\n\nvec4 GetScatteringTextureUvwzFromRMuMuSNu(const AtmosphereParameters atmosphere,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu, \n\tconst bool ray_r_mu_intersects_ground) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tassert(mu_s >= -1.0 && mu_s <= 1.0);\n\tassert(nu >= -1.0 && nu <= 1.0);\n\n\t// Distance to top atmosphere boundary for a horizontal ray at ground level.\n\tLength H = sqrt(atmosphere.top_radius * atmosphere.top_radius - \n\t\tatmosphere.bottom_radius * atmosphere.bottom_radius);\n\t// Distance to the horizon.\n\tLength rho = \n\t\tSafeSqrt(r * r - atmosphere.bottom_radius * atmosphere.bottom_radius);\n\tNumber u_r = GetTextureCoordFromUnitRange(rho / H, SCATTERING_TEXTURE_R_SIZE);\n\n\t#if INSCATTER_MAPPING == 1\n\t\t// Discriminant of the quadratic equation for the intersections of the ray\n\t\t\t// (r,mu) with the ground (see RayIntersectsGround).\n\t\tLength r_mu = r * mu;\n\t\tArea discriminant = \n\t\t\tr_mu * r_mu - r * r + atmosphere.bottom_radius * atmosphere.bottom_radius;\n\t\tNumber u_mu;\n\t\tif (ray_r_mu_intersects_ground) {\n\t\t\t// Distance to the ground for the ray (r,mu), and its minimum and maximum\n\t\t\t\t// values over all mu - obtained for (r,-1) and (r,mu_horizon).\n\t\t\tLength d = -r_mu - SafeSqrt(discriminant);\n\t\t\tLength d_min = r - atmosphere.bottom_radius;\n\t\t\tLength d_max = rho;\n\t\t\tu_mu = 0.5 - 0.5 * GetTextureCoordFromUnitRange(d_max == d_min ? 0.0 : \n\t\t\t\t(d - d_min) / (d_max - d_min), SCATTERING_TEXTURE_MU_SIZE / 2);\n\t\t} else {\n\t\t\t// Distance to the top atmosphere boundary for the ray (r,mu), and its\n\t\t\t// minimum and maximum values over all mu - obtained for (r,1) and\n\t\t\t// (r,mu_horizon).\n\t\t\tLength d = -r_mu + SafeSqrt(discriminant + H * H);\n\t\t\tLength d_min = atmosphere.top_radius - r;\n\t\t\tLength d_max = rho + H;\n\t\t\tu_mu = 0.5 + 0.5 * GetTextureCoordFromUnitRange(\n\t\t\t\t(d - d_min) / (d_max - d_min), SCATTERING_TEXTURE_MU_SIZE / 2);\n\t\t}\n\n\t\tLength d = DistanceToTopAtmosphereBoundary(\n\t\t\tatmosphere, atmosphere.bottom_radius, mu_s);\n\t\tLength d_min = atmosphere.top_radius - atmosphere.bottom_radius;\n\t\tLength d_max = H;\n\t\tNumber a = (d - d_min) / (d_max - d_min);\n\t\tLength D = DistanceToTopAtmosphereBoundary(\n\t\t\tatmosphere, atmosphere.bottom_radius, atmosphere.mu_s_min);\n\t\tNumber A = (D - d_min) / (d_max - d_min);\n\t\t// An ad-hoc function equal to 0 for mu_s = mu_s_min (because then d = D and\n\t\t// thus a = A), equal to 1 for mu_s = 1 (because then d = d_min and thus\n\t\t// a = 0), and with a large slope around mu_s = 0, to get more texture\n\t\t// samples near the horizon.\n\t\tNumber u_mu_s = GetTextureCoordFromUnitRange(\n\t\t\tmax(1.0 - a / A, 0.0) / (1.0 + a), SCATTERING_TEXTURE_MU_S_SIZE);\n\t#else\n\t\tNumber u_mu = GetTextureCoordFromUnitRange(\n\t\t\t(mu + 1.0) / 2.0, SCATTERING_TEXTURE_MU_SIZE);\n\t\tNumber u_mu_s = GetTextureCoordFromUnitRange(\n\t\t\tmax(mu_s + 0.2, 0.0) / 1.2, SCATTERING_TEXTURE_MU_S_SIZE);\n\t#endif\n\n\tNumber u_nu = (nu + 1.0) / 2.0;\n\treturn vec4(u_nu, u_mu_s, u_mu, u_r);\n}\n\nvec2 GetIrradianceTextureUvFromRMuS(const AtmosphereParameters atmosphere,\n\tconst Length r, const Number mu_s) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu_s >= -1.0 && mu_s <= 1.0);\n\tNumber x_r = (r - atmosphere.bottom_radius) /\n\t\t(atmosphere.top_radius - atmosphere.bottom_radius);\n\tNumber x_mu_s = mu_s * 0.5 + 0.5;\n\treturn vec2(GetTextureCoordFromUnitRange(x_mu_s, IRRADIANCE_TEXTURE_WIDTH),\n\t\t\t\tGetTextureCoordFromUnitRange(x_r, IRRADIANCE_TEXTURE_HEIGHT));\n}\n\nIrradianceSpectrum GetIrradiance(\n\tconst AtmosphereParameters atmosphere,\n\tconst IrradianceTexture irradiance_texture,\n\tconst Length r, const Number mu_s) {\n\tvec2 uv = GetIrradianceTextureUvFromRMuS(atmosphere, r, mu_s);\n\treturn IrradianceSpectrum(texture2D(irradiance_texture, uv));\n}\n",o="\nvec3 GetExtrapolatedSingleMieScattering(\n\tconst AtmosphereParameters atmosphere, const vec4 scattering) {\n\t// Algebraically this can never be negative, but rounding errors can produce\n\t// that effect for sufficiently short view rays.\n\t// @shotamatsuda: Avoid division by infinitesimal values.\n\t// See https://github.com/takram-design-engineering/three-geospatial/issues/47\n\tif (scattering.r < 1e-5) {\n\t\treturn vec3(0.0);\n\t}\n\treturn scattering.rgb * scattering.a / scattering.r *\n\t\t(atmosphere.rayleigh_scattering.r / atmosphere.mie_scattering.r) *\n\t\t(atmosphere.mie_scattering / atmosphere.rayleigh_scattering);\n}\n\nIrradianceSpectrum GetCombinedScattering(\n\tconst AtmosphereParameters atmosphere,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu, \n\tconst bool ray_r_mu_intersects_ground, out IrradianceSpectrum single_mie_scattering) {\n\tvec4 uvwz = GetScatteringTextureUvwzFromRMuMuSNu(\n\t\tatmosphere, r, mu, mu_s, nu, ray_r_mu_intersects_ground);\n\n\tNumber tex_coord_x = uvwz.x * Number(SCATTERING_TEXTURE_NU_SIZE - 1);\n\tNumber tex_x = floor(tex_coord_x);\n\tNumber lerp = tex_coord_x - tex_x;\n\tvec3 uvw0 = vec3((tex_x + uvwz.y) / Number(SCATTERING_TEXTURE_NU_SIZE),\n\t\tuvwz.z, uvwz.w);\n\tvec3 uvw1 = vec3((tex_x + 1.0 + uvwz.y) / Number(SCATTERING_TEXTURE_NU_SIZE),\n\t\tuvwz.z, uvwz.w);\n\n\tvec4 combined_scattering =\n\t\ttexture(scattering_texture, uvw0) * (1.0 - lerp) +\n\t\ttexture(scattering_texture, uvw1) * lerp;\n\tIrradianceSpectrum scattering = IrradianceSpectrum(combined_scattering);\n\tsingle_mie_scattering =\n\t\tGetExtrapolatedSingleMieScattering(atmosphere, combined_scattering);\n\n\treturn scattering;\n}\n\nvec3 GetSkyRadiance(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tPosition camera, const Direction view_ray, \n\tconst Direction sun_direction, const bool clamp_mu_at_horizon, \n\tout DimensionlessSpectrum transmittance) {\n\t// Compute the distance to the top atmosphere boundary along the view ray,\n\t// assuming the viewer is in space (or NaN if the view ray does not intersect\n\t// the atmosphere).\n\tLength r = length(camera);\n\t// @shotamatsuda: For rendering points below the bottom atmosphere.\n\tif (!clamp_mu_at_horizon && r < atmosphere.bottom_radius) {\n\t\tr = atmosphere.bottom_radius;\n\t\tcamera = normalize(camera) * r;\n\t}\n\tLength rmu = dot(camera, view_ray);\n\t// @shotamatsuda: Use SafeSqrt instead.\n\t// See: https://github.com/takram-design-engineering/three-geospatial/pull/26\n\tLength distance_to_top_atmosphere_boundary = -rmu -\n\t\tSafeSqrt(rmu * rmu - r * r +\n\t\t\tatmosphere.top_radius * atmosphere.top_radius);\n\t// If the viewer is in space and the view ray intersects the atmosphere, move\n\t// the viewer to the top atmosphere boundary (along the view ray):\n\tif (distance_to_top_atmosphere_boundary > 0.0 * m) {\n\t\tcamera = camera + view_ray * distance_to_top_atmosphere_boundary;\n\t\tr = atmosphere.top_radius;\n\t\trmu += distance_to_top_atmosphere_boundary;\n\t} else if (r > atmosphere.top_radius) {\n\t\t// If the view ray does not intersect the atmosphere, simply return 0.\n\t\ttransmittance = DimensionlessSpectrum(1.0);\n\t\treturn RadianceSpectrum(0.0 * watt_per_square_meter_per_sr_per_nm);\n\t}\n\t// Compute the r, mu, mu_s and nu parameters needed for the texture lookups.\n\tNumber mu = rmu / r;\n\t// @shotamatsuda: For rendering points below the bottom atmosphere.\n\tif (clamp_mu_at_horizon) {\n\t\tNumber mu_horizon = -SafeSqrt(1.0 -\n\t\t\t(atmosphere.bottom_radius * atmosphere.bottom_radius) / (r * r));\n\t\tconst Number eps = 0.001;\n\t\tmu = max(rmu / r, mu_horizon + eps);\n\t}\n\tNumber mu_s = dot(camera, sun_direction) / r;\n\tNumber nu = dot(view_ray, sun_direction);\n\tbool ray_r_mu_intersects_ground = RayIntersectsGround(atmosphere, r, mu);\n\n\ttransmittance = ray_r_mu_intersects_ground ? DimensionlessSpectrum(0.0) :\n\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\tatmosphere, transmittance_texture, r, mu);\n\n\tIrradianceSpectrum single_mie_scattering;\n\tIrradianceSpectrum scattering;\n\t\n\tscattering = GetCombinedScattering(\n\t\tatmosphere, scattering_texture,\n\t\tr, mu, mu_s, nu, ray_r_mu_intersects_ground,\n\t\tsingle_mie_scattering);\n\n\treturn scattering * RayleighPhaseFunction(nu) + single_mie_scattering * \n\t\tMiePhaseFunction(atmosphere.mie_phase_function_g, nu);\n}\n\n// @shotamatsuda: Returns the point on the ray closest to the origin.\nvec3 ClosestPointOnRay(const Position camera, const Position point) {\n\tPosition ray = point - camera;\n\tNumber t = clamp(-dot(camera, ray) / dot(ray, ray), 0.0, 1.0);\n\treturn camera + t * ray;\n}\n\nvec2 RaySphereIntersections(\n\tconst Position camera, const Direction direction, const Length radius) {\n\tfloat b = 2.0 * dot(direction, camera);\n\tfloat c = dot(camera, camera) - radius * radius;\n\tfloat discriminant = b * b - 4.0 * c;\n\tfloat Q = sqrt(discriminant);\n\treturn vec2(-b - Q, -b + Q) * 0.5;\n}\n\n// @shotamatsuda: Clip the view ray at the bottom atmosphere boundary.\nbool ClipAtBottomAtmosphere(\n\tconst AtmosphereParameters atmosphere,\n\tconst Direction view_ray, inout Position camera, inout Position point) {\n\tconst Length eps = 0.0;\n\tLength bottom_radius = atmosphere.bottom_radius + eps;\n\tLength r_camera = length(camera);\n\tLength r_point = length(point);\n\tbool camera_below = r_camera < bottom_radius;\n\tbool point_below = r_point < bottom_radius;\n\tif (camera_below && point_below) {\n\t\treturn false;\n\t}\n\tvec2 t = RaySphereIntersections(camera, view_ray, bottom_radius);\n\tPosition intersection = camera + view_ray * (camera_below ? t.y : t.x);\n\tif (camera_below) {\n\t\tcamera = intersection;\n\t} else if (point_below) {\n\t\tpoint = intersection;\n\t}\n\treturn true;\n}\n\nRadianceSpectrum GetSkyRadianceToPoint(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tPosition camera, Position point,\n\tconst Direction sun_direction, out DimensionlessSpectrum transmittance) {\n\t// @shotamatsuda: Avoid artifacts when the ray does not intersect the top\n\t// atmosphere boundary.\n\tif (length(ClosestPointOnRay(camera, point)) > atmosphere.top_radius) {\n\t\ttransmittance = vec3(1.0);\n\t\treturn vec3(0.0);\n\t}\n\n\tDirection view_ray = normalize(point - camera);\n\tif (!ClipAtBottomAtmosphere(atmosphere, view_ray, camera, point)) {\n\t\ttransmittance = vec3(1.0);\n\t\treturn vec3(0.0);\n\t}\n\n\t// Compute the distance to the top atmosphere boundary along the view ray,\n\t// assuming the viewer is in space (or NaN if the view ray does not intersect\n\t// the atmosphere).\n\tLength r = length(camera);\n\tLength rmu = dot(camera, view_ray);\n\t// @shotamatsuda: Use SafeSqrt instead.\n\t// See: https://github.com/takram-design-engineering/three-geospatial/pull/26\n\tLength distance_to_top_atmosphere_boundary = -rmu - \n\t\tSafeSqrt(rmu * rmu - r * r + \n\t\t\tatmosphere.top_radius * atmosphere.top_radius);\n\t// If the viewer is in space and the view ray intersects the atmosphere, move\n\t// the viewer to the top atmosphere boundary (along the view ray):\n\tif (distance_to_top_atmosphere_boundary > 0.0 * m) {\n\t\tcamera = camera + view_ray * distance_to_top_atmosphere_boundary;\n\t\tr = atmosphere.top_radius;\n\t\trmu += distance_to_top_atmosphere_boundary;\n\t}\n\n\t// Compute the r, mu, mu_s and nu parameters for the first texture lookup.\n\tNumber mu = rmu / r;\n\tNumber mu_s = dot(camera, sun_direction) / r;\n\tNumber nu = dot(view_ray, sun_direction);\n\tLength d = length(point - camera);\n\tbool ray_r_mu_intersects_ground = RayIntersectsGround(atmosphere, r, mu);\n\n\t// @shotamatsuda: Hack to avoid rendering artifacts near the horizon, due to\n\t// finite atmosphere texture resolution and finite floating point precision.\n\t// See: https://github.com/ebruneton/precomputed_atmospheric_scattering/pull/32\n\tif (!ray_r_mu_intersects_ground) {\n\t\tNumber mu_horizon = -SafeSqrt(1.0 - \n\t\t\t(atmosphere.bottom_radius * atmosphere.bottom_radius) / (r * r));\n\t\tconst Number eps = 0.004;\n\t\tmu = max(mu, mu_horizon + eps);\n\t}\n\n\ttransmittance = GetTransmittance(atmosphere, transmittance_texture,\n\t\tr, mu, d, ray_r_mu_intersects_ground);\n\n\tIrradianceSpectrum single_mie_scattering;\n\tIrradianceSpectrum scattering = GetCombinedScattering(\n\t\tatmosphere, scattering_texture,\n\t\tr, mu, mu_s, nu, ray_r_mu_intersects_ground,\n\t\tsingle_mie_scattering);\n\n\t// Compute the r, mu, mu_s and nu parameters for the second texture lookup.\n\t// If shadow_length is not 0 (case of light shafts), we want to ignore the\n\t// scattering along the last shadow_length meters of the view ray, which we\n\t// do by subtracting shadow_length from d (this way scattering_p is equal to\n\t// the S|x_s=x_0-lv term in Eq. (17) of our paper).\n\td = max(d, 0.0 * m);\n\tLength r_p = ClampRadius(atmosphere, sqrt(d * d + 2.0 * r * mu * d + r * r));\n\tNumber mu_p = (r * mu + d) / r_p;\n\tNumber mu_s_p = (r * mu_s + d * nu) / r_p;\n\n\tIrradianceSpectrum single_mie_scattering_p;\n\tIrradianceSpectrum scattering_p = GetCombinedScattering(\n\t\tatmosphere, scattering_texture,\n\t\tr_p, mu_p, mu_s_p, nu, ray_r_mu_intersects_ground,\n\t\tsingle_mie_scattering_p);\n\n\t// Combine the lookup results to get the scattering between camera and point.\n\tscattering = scattering - transmittance * scattering_p;\n\tsingle_mie_scattering = single_mie_scattering - transmittance * single_mie_scattering_p;\n\n\tsingle_mie_scattering = GetExtrapolatedSingleMieScattering(\n\t\tatmosphere, vec4(scattering, single_mie_scattering.r));\n\n\t// Hack to avoid rendering artifacts when the sun is below the horizon.\n\tsingle_mie_scattering = single_mie_scattering *\n\t\tsmoothstep(Number(0.0), Number(0.01), mu_s);\n\n\treturn scattering * RayleighPhaseFunction(nu) + single_mie_scattering *\n\t\tMiePhaseFunction(atmosphere.mie_phase_function_g, nu);\n}\n\nIrradianceSpectrum GetSunAndSkyIrradiance(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst IrradianceTexture irradiance_texture,\n\tconst Position point, const Direction normal, const Direction sun_direction,\n\tout IrradianceSpectrum sky_irradiance) {\n\tLength r = length(point);\n\tNumber mu_s = dot(point, sun_direction) / r;\n\n\t// Indirect irradiance (approximated if the surface is not horizontal).\n\tsky_irradiance = GetIrradiance(atmosphere, irradiance_texture, r, mu_s) *\n\t\t(1.0 + dot(normal, point) / r) * 0.5;\n\n\t// Direct irradiance.\n\treturn atmosphere.solar_irradiance *\n\t\tGetTransmittanceToSun(\n\t\t\tatmosphere, transmittance_texture, r, mu_s) *\n\t\tmax(dot(normal, sun_direction), 0.0);\n}\n\nLuminance3 GetSkyLuminance(\n\tconst Position camera, Direction view_ray,\n\tconst Direction sun_direction, out DimensionlessSpectrum transmittance) {\n\t#ifdef GROUND\n\t\tconst bool clamp_mu_at_horizon = false;\n\t#else\n\t\tconst bool clamp_mu_at_horizon = true;\n\t#endif\n\n\treturn GetSkyRadiance(ATMOSPHERE, transmittance_texture,\n\t\tscattering_texture,\n\t\tcamera, view_ray,\n\t\tsun_direction, clamp_mu_at_horizon,\n\t\ttransmittance) * SKY_SPECTRAL_RADIANCE_TO_LUMINANCE;\n}\n\nLuminance3 GetSkyLuminanceToPoint(\n\tconst Position camera, const Position point,\n\tconst Direction sun_direction, out DimensionlessSpectrum transmittance) {\n\treturn GetSkyRadianceToPoint(ATMOSPHERE, transmittance_texture,\n\t\tscattering_texture,\n\t\tcamera, point, sun_direction, transmittance) *\n\t\tSKY_SPECTRAL_RADIANCE_TO_LUMINANCE;\n}\n\nIlluminance3 GetSunAndSkyIlluminance(\n\tconst Position p, const Direction normal, const Direction sun_direction,\n\tout IrradianceSpectrum sky_irradiance) {\n\tIrradianceSpectrum sun_irradiance = GetSunAndSkyIrradiance(\n\t\tATMOSPHERE, transmittance_texture, irradiance_texture, p, normal,\n\t\tsun_direction, sky_irradiance);\n\tsky_irradiance *= SKY_SPECTRAL_RADIANCE_TO_LUMINANCE;\n\treturn sun_irradiance * SUN_SPECTRAL_RADIANCE_TO_LUMINANCE;\n}\n\n#define GetSkyRadiance GetSkyLuminance\n#define GetSkyRadianceToPoint GetSkyLuminanceToPoint\n#define GetSunAndSkyIrradiance GetSunAndSkyIlluminance\n",i=.001,s=`\n#define IRRADIANCE_TEXTURE_WIDTH ${64..toFixed(0)}\n#define IRRADIANCE_TEXTURE_HEIGHT ${16..toFixed(0)}\n#define SCATTERING_TEXTURE_R_SIZE ${32..toFixed(0)}\n#define SCATTERING_TEXTURE_MU_SIZE ${128..toFixed(0)}\n#define SCATTERING_TEXTURE_MU_S_SIZE ${32..toFixed(0)}\n#define SCATTERING_TEXTURE_NU_SIZE ${8..toFixed(0)}\n#define TRANSMITTANCE_TEXTURE_WIDTH ${256..toFixed(0)}\n#define TRANSMITTANCE_TEXTURE_HEIGHT ${64..toFixed(0)}\n#define METER_TO_LENGTH_UNIT ${i.toFixed(7)}\n\n// Half heights for the atmosphere air density (HR) and particle density (HM)\n// This is the height in km that half the particles are found below\nconst float HR = 8.0;\nconst float HM = 1.2;\n`,m=new e.Vector3(.2126,.7152,.0722);class u{constructor(t,e,n,r,a){this.width=t,this.expTerm=e,this.expScale=n,this.linearTerm=r,this.constantTerm=a}toUniform(){return{width:this.width,exp_term:this.expTerm,exp_scale:this.expScale,linear_term:this.linearTerm,constant_term:this.constantTerm}}}class c{constructor(){this.solarIrradiance=new e.Vector3(1.474,1.8504,1.91198),this.sunAngularRadius=.004675,this.bottomRadius=636e4,this.topRadius=642e4,this.rayleighDensity=[new u(0,0,0,0,0),new u(0,1,-.125,0,0)],this.rayleighScattering=new e.Vector3(.005802,.013558,.0331),this.mieDensity=[new u(0,0,0,0,0),new u(0,1,-.833333,0,0)],this.mieScattering=new e.Vector3(.003996,.003996,.003996),this.mieExtinction=new e.Vector3(.00444,.00444,.00444),this.miePhaseFunctionG=.8,this.absorptionDensity=[new u(25,0,0,1/15,-2/3),new u(0,0,0,-1/15,8/3)],this.absorptionExtinction=new e.Vector3(65e-5,.001881,85e-6),this.groundAlbedo=new e.Color3(.1,.1,.1),this.muSMin=Math.cos(120*Math.PI/180),this.sunRadianceToLuminance=new e.Vector3(98242.786222,69954.398112,66475.012354),this.skyRadianceToLuminance=new e.Vector3(114974.916437,71305.954816,65310.548555);const t=m.dot(this.sunRadianceToLuminance);this.sunRadianceToRelativeLuminance=this.sunRadianceToLuminance.clone().multiplyScalar(1/t),this.skyRadianceToRelativeLuminance=this.skyRadianceToLuminance.clone().multiplyScalar(1/t)}toUniform(){return{solar_irradiance:this.solarIrradiance.toArray(),sun_angular_radius:this.sunAngularRadius,bottom_radius:this.bottomRadius*i,top_radius:this.topRadius*i,rayleigh_density:{layers:this.rayleighDensity.map(t=>t.toUniform())},rayleigh_scattering:this.rayleighScattering.toArray(),mie_density:{layers:this.mieDensity.map(t=>t.toUniform())},mie_scattering:this.mieScattering.toArray(),mie_extinction:this.mieExtinction.toArray(),mie_phase_function_g:this.miePhaseFunctionG,absorption_density:{layers:this.absorptionDensity.map(t=>t.toUniform())},absorption_extinction:this.absorptionExtinction.toArray(),ground_albedo:this.groundAlbedo.toArray(),mu_s_min:this.muSMin}}}c.DEFAULT=new c;const _={name:"atmos_sky",defines:{TRANSMITTANCE_MAPPING:1,INSCATTER_MAPPING:1,TONE_MAPPING:5,SRGB_OUTPUT:!0,GROUND_ALBEDO:!0,SKY_SUNDISK:!0},uniforms:{ATMOSPHERE:c.DEFAULT.toUniform(),SUN_SPECTRAL_RADIANCE_TO_LUMINANCE:[0,0,0],SKY_SPECTRAL_RADIANCE_TO_LUMINANCE:[0,0,0],scattering_texture:null,transmittance_texture:null,irradiance_texture:null,cameraPosition:[0,0,0],sunDirection:[0,0,0],worldToECEFMatrix:new Array(16),altitudeCorrection:[0,0,0],toneMappingExposure:10,sunDiskSize:1},vertexShader:`\n\t\t#define METER_TO_LENGTH_UNIT ${i.toFixed(7)}\n\n\t\t\t\tattribute vec3 a_Position;\n\n\t\tuniform mat4 u_Projection;\n\t\tuniform mat4 u_View;\n\t\tuniform mat4 u_Model;\n\n\t\tuniform vec3 cameraPosition;\n\t\tuniform mat4 worldToECEFMatrix;\n\t\tuniform vec3 altitudeCorrection;\n\n\t\tvarying vec3 vCameraPosition;\n\t\tvarying vec3 vRayDirection;\n\n\t\tvoid getCameraRay(out vec3 origin, out vec3 direction) {\n\t\t\tmat4 inverseProjectionMatrix = inverse(u_Projection);\n\t\t\tmat4 inverseViewMatrix = inverse(u_Model * u_View); // pre-multiplied by model matrix in case use anchorMatrix\n\n\t\t\tbool isPerspective = inverseProjectionMatrix[2][3] != 0.0; // 4th entry in the 3rd column\n\n\t\t\tif (isPerspective) {\n\t\t\t\t// Calculate the camera ray for a perspective camera.\n\t\t\t\tvec4 viewPosition = inverseProjectionMatrix * vec4(a_Position.xzy, 1.0);\n\t\t\t\tvec4 worldDirection = inverseViewMatrix * vec4(viewPosition.xyz, 0.0);\n\t\t\t\torigin = cameraPosition;\n\t\t\t\tdirection = worldDirection.xyz;\n\t\t\t} else {\n\t\t\t\t// Unprojected points to calculate direction.\n\t\t\t\tvec4 nearPoint = inverseProjectionMatrix * vec4(a_Position.xz, -1.0, 1.0);\n\t\t\t\tvec4 farPoint = inverseProjectionMatrix * vec4(a_Position.xz, -0.9, 1.0);\n\t\t\t\tnearPoint /= nearPoint.w;\n\t\t\t\tfarPoint /= farPoint.w;\n\n\t\t\t\t// Calculate world values\n\t\t\t\tvec4 worldDirection = inverseViewMatrix * vec4(farPoint.xyz - nearPoint.xyz, 0.0);\n\t\t\t\tvec4 worldOrigin = inverseViewMatrix * nearPoint;\n\n\t\t\t\t// Outputs\n\t\t\t\tdirection = worldDirection.xyz;\n\t\t\t\torigin = worldOrigin.xyz;\n\t\t\t}\n\t\t}\n\t\t\t\t\n\t\t\t\tvoid main() {\n\t\t\tvec3 direction, origin;\n\t\t\t\tgetCameraRay(origin, direction);\n\n\t\t\tvec3 cameraPositionECEF = (worldToECEFMatrix * vec4(origin, 1.0)).xyz;\n\n\t\t\tvCameraPosition = (cameraPositionECEF + altitudeCorrection) * METER_TO_LENGTH_UNIT;\n\t\t\tvRayDirection = (worldToECEFMatrix * vec4(direction, 0.0)).xyz;\n\n\t\t\tgl_Position = vec4(a_Position.xz, 1.0, 1.0);\n\t\t\t\t}\n\t\t`,fragmentShader:`\n\t\t${s}\n\t\t${r}\n\t\t${a}\n\t\t\nfloat RaySphereFirstIntersection(vec3 origin, vec3 direction, vec3 center, float radius) {\n\tvec3 a = origin - center;\n\tfloat b = 2.0 * dot(direction, a);\n\tfloat c = dot(a, a) - radius * radius;\n\tfloat discriminant = b * b - 4.0 * c;\n\treturn discriminant < 0.0\n\t\t? -1.0\n\t\t: (-b - sqrt(discriminant)) * 0.5;\n}\n\nfloat RaySphereFirstIntersection(vec3 origin, vec3 direction, float radius) {\n\treturn RaySphereFirstIntersection(origin, direction, vec3(0.0), radius);\n}\n\n\n\t\tuniform AtmosphereParameters ATMOSPHERE;\n\t\tuniform vec3 SUN_SPECTRAL_RADIANCE_TO_LUMINANCE;\n\t\tuniform vec3 SKY_SPECTRAL_RADIANCE_TO_LUMINANCE;\n\n\t\tuniform highp sampler3D scattering_texture;\n\t\t\t\tuniform sampler2D transmittance_texture;\n\t\tuniform sampler2D irradiance_texture;\n\n\t\t${o}\n\n\t\t\t\tuniform float toneMappingExposure;\n\n\t\tuniform vec3 sunDirection;\n\t\tuniform float sunDiskSize;\n\n\t\tvarying vec3 vCameraPosition;\n\t\tvarying vec3 vRayDirection;\n\n\t\t\n#if TONE_MAPPING == 0\n\t// exposure only\n\tvec3 ToneMapping(vec3 color) {\n\t\treturn saturate(toneMappingExposure * color);\n\t}\n#elif TONE_MAPPING == 1\n\t// source: https://www.cs.utah.edu/docs/techreports/2002/pdf/UUCS-02-001.pdf\n\tvec3 ToneMapping(vec3 color) {\n\t\tcolor *= toneMappingExposure;\n\t\treturn saturate(color / (vec3(1.0) + color));\n\t}\n#elif TONE_MAPPING == 2\n\t// source: http://filmicworlds.com/blog/filmic-tonemapping-operators/\n\tvec3 ToneMapping(vec3 color) {\n\t\t// optimized filmic operator by Jim Hejl and Richard Burgess-Dawson\n\t\tcolor *= toneMappingExposure;\n\t\tcolor = max(vec3(0.0), color - 0.004);\n\t\treturn pow((color * (6.2 * color + 0.5)) / (color * (6.2 * color + 1.7) + 0.06), vec3(2.2));\n\t}\n#elif TONE_MAPPING == 3\n\t// source: https://github.com/selfshadow/ltc_code/blob/master/webgl/shaders/ltc/ltc_blit.fs\n\tvec3 RRTAndODTFit(vec3 v) {\n\t\tvec3 a = v * (v + 0.0245786) - 0.000090537;\n\t\tvec3 b = v * (0.983729 * v + 0.4329510) + 0.238081;\n\t\treturn a / b;\n\t}\n\n\t// this implementation of ACES is modified to accommodate a brighter viewing environment.\n\t// the scale factor of 1/0.6 is subjective. see discussion in https://github.com/mrdoob/three.js/pull/19621.\n\tvec3 ToneMapping(vec3 color) {\n\t\t// sRGB => XYZ => D65_2_D60 => AP1 => RRT_SAT\n\t\tconst mat3 ACESInputMat = mat3(\n\t\t\tvec3(0.59719, 0.07600, 0.02840), // transposed from source\n\t\t\tvec3(0.35458, 0.90834, 0.13383),\n\t\t\tvec3(0.04823, 0.01566, 0.83777)\n\t\t);\n\t\t// ODT_SAT => XYZ => D60_2_D65 => sRGB\n\t\tconst mat3 ACESOutputMat = mat3(\n\t\t\tvec3( 1.60475, -0.10208, -0.00327), // transposed from source\n\t\t\tvec3(-0.53108,\t1.10813, -0.07276),\n\t\t\tvec3(-0.07367, -0.00605,\t1.07602)\n\t\t);\n\t\tcolor *= toneMappingExposure / 0.6;\n\t\tcolor = ACESInputMat * color;\n\t\t// Apply RRT and ODT\n\t\tcolor = RRTAndODTFit(color);\n\t\tcolor = ACESOutputMat * color;\n\t\t// Clamp to [0, 1]\n\t\treturn saturate(color);\n\t}\n#elif TONE_MAPPING == 4\n\tvec3 ToneMapping(vec3 color) {\n\t\tconst float StartCompression = 0.8 - 0.04;\n\t\tconst float Desaturation = 0.15;\n\t\tcolor *= toneMappingExposure;\n\t\tfloat x = min(color.r, min(color.g, color.b));\n\t\tfloat offset = x < 0.08 ? x - 6.25 * x * x : 0.04;\n\t\tcolor -= offset;\n\t\tfloat peak = max(color.r, max(color.g, color.b));\n\t\tif (peak < StartCompression) return color;\n\t\tfloat d = 1. - StartCompression;\n\t\tfloat newPeak = 1. - d * d / (peak + d - StartCompression);\n\t\tcolor *= newPeak / peak;\n\t\tfloat g = 1. - 1. / (Desaturation * (peak - newPeak) + 1.);\n\t\treturn mix(color, vec3(newPeak), g);\n\t}\n#elif TONE_MAPPING == 5\n\t// Matrices for rec 2020 <> rec 709 color space conversion\n\t// matrix provided in row-major order so it has been transposed\n\t// https://www.itu.int/pub/R-REP-BT.2407-2017\n\tconst mat3 LINEAR_REC2020_TO_LINEAR_SRGB = mat3(\n\t\tvec3(1.6605, -0.1246, -0.0182),\n\t\tvec3(-0.5876, 1.1329, -0.1006),\n\t\tvec3(-0.0728, -0.0083, 1.1187)\n\t);\n\n\tconst mat3 LINEAR_SRGB_TO_LINEAR_REC2020 = mat3(\n\t\tvec3(0.6274, 0.0691, 0.0164),\n\t\tvec3(0.3293, 0.9195, 0.0880),\n\t\tvec3(0.0433, 0.0113, 0.8956)\n\t);\n\n\t// https://iolite-engine.com/blog_posts/minimal_agx_implementation\n\t// Mean error^2: 3.6705141e-06\n\tvec3 agxDefaultContrastApprox(vec3 x) {\n\t\tvec3 x2 = x * x;\n\t\tvec3 x4 = x2 * x2;\n\n\t\treturn + 15.5 * x4 * x2\n\t\t\t- 40.14 * x4 * x\n\t\t\t+ 31.96 * x4\n\t\t\t- 6.868 * x2 * x\n\t\t\t+ 0.4298 * x2\n\t\t\t+ 0.1191 * x\n\t\t\t- 0.00232;\n\t}\n\n\t// AgX Tone Mapping implementation based on Filament, which in turn is based\n\t// on Blender's implementation using rec 2020 primaries\n\t// https://github.com/google/filament/pull/7236\n\t// Inputs and outputs are encoded as Linear-sRGB.\n\tvec3 ToneMapping(vec3 color) {\n\t\t// AgX constants\n\t\tconst mat3 AgXInsetMatrix = mat3(\n\t\t\tvec3(0.856627153315983, 0.137318972929847, 0.11189821299995),\n\t\t\tvec3(0.0951212405381588, 0.761241990602591, 0.0767994186031903),\n\t\t\tvec3(0.0482516061458583, 0.101439036467562, 0.811302368396859)\n\t\t);\n\n\t\t// explicit AgXOutsetMatrix generated from Filaments AgXOutsetMatrixInv\n\t\tconst mat3 AgXOutsetMatrix = mat3(\n\t\t\tvec3(1.1271005818144368, -0.1413297634984383, -0.14132976349843826),\n\t\t\tvec3(-0.11060664309660323, 1.157823702216272, -0.11060664309660294),\n\t\t\tvec3(-0.016493938717834573, -0.016493938717834257, 1.2519364065950405)\n\t\t);\n\n\t\t// LOG2_MIN\t\t\t= -10.0\n\t\t// LOG2_MAX\t\t\t=\t+6.5\n\t\t// MIDDLE_GRAY\t =\t0.18\n\t\tconst float AgxMinEv = -12.47393;\t// log2(pow(2, LOG2_MIN) * MIDDLE_GRAY)\n\t\tconst float AgxMaxEv = 4.026069;\t // log2(pow(2, LOG2_MAX) * MIDDLE_GRAY)\n\n\t\tcolor *= toneMappingExposure;\n\n\t\tcolor = LINEAR_SRGB_TO_LINEAR_REC2020 * color;\n\n\t\tcolor = AgXInsetMatrix * color;\n\n\t\t// Log2 encoding\n\t\tcolor = max(color, 1e-10); // avoid 0 or negative numbers for log2\n\t\tcolor = log2(color);\n\t\tcolor = (color - AgxMinEv) / (AgxMaxEv - AgxMinEv);\n\n\t\tcolor = clamp(color, 0.0, 1.0);\n\n\t\t// Apply sigmoid\n\t\tcolor = agxDefaultContrastApprox(color);\n\n\t\t// Apply AgX look\n\t\t// v = agxLook(v, look);\n\n\t\tcolor = AgXOutsetMatrix * color;\n\n\t\t// Linearize\n\t\tcolor = pow(max(vec3(0.0), color), vec3(2.2));\n\n\t\tcolor = LINEAR_REC2020_TO_LINEAR_SRGB * color;\n\n\t\t// Gamut mapping. Simple clamp for now.\n\t\tcolor = clamp(color, 0.0, 1.0);\n\n\t\treturn color;\n\t}\n#elif TONE_MAPPING == 6\n\tvec3 ToneMapping(vec3 color) {\n\t\tcolor *= toneMappingExposure;\n\t\tcolor.r = mix(1.0 - exp(-color.r), pow(color.r * 0.38317, 1.0 / 2.2), step(color.r, 1.413));\n\t\tcolor.g = mix(1.0 - exp(-color.g), pow(color.g * 0.38317, 1.0 / 2.2), step(color.g, 1.413));\n\t\tcolor.b = mix(1.0 - exp(-color.b), pow(color.b * 0.38317, 1.0 / 2.2), step(color.b, 1.413));\n\t\treturn color;\n\t}\n#else\n\tvec3 ToneMapping(vec3 color) {\n\t\treturn color; // no tone mapping\n\t}\n#endif\n\n\n\t\t#include <dithering_pars_frag>\n\n\t\t\t\tvoid main() {\n\t\t\tvec3 cameraPosition = vCameraPosition;\n\t\t\t\t\t\tvec3 rayDirection = normalize(vRayDirection);\n\n\t\t\tvec4 outputColor;\n\t\t\tvec3 transmittance;\n\n\t\t\t#ifdef GROUND_ALBEDO\n\t\t\t\tfloat r = length(cameraPosition);\n\t\t\t\t\tfloat mu = dot(cameraPosition, rayDirection) / r;\n\t\t\t\tbool ray_r_mu_intersects_ground = RayIntersectsGround(ATMOSPHERE, r, mu);\n\t\t\t\tif (ray_r_mu_intersects_ground) {\n\t\t\t\t\tfloat distance_to_ground = RaySphereFirstIntersection(\n\t\t\t\t\t\tcameraPosition,\n\t\t\t\t\t\trayDirection,\n\t\t\t\t\t\tATMOSPHERE.bottom_radius);\n\t\t\t\t\tvec3 groundPosition = rayDirection * distance_to_ground + cameraPosition;\n\t\t\t\t\tvec3 surfaceNormal = normalize(groundPosition);\n\t\t\t\t\tvec3 skyIrradiance;\n\t\t\t\t\tvec3 sunIrradiance = GetSunAndSkyIrradiance(\n\t\t\t\t\t\tcameraPosition,\n\t\t\t\t\t\tsurfaceNormal, \n\t\t\t\t\t\tsunDirection, \n\t\t\t\t\t\tskyIrradiance\n\t\t\t\t\t);\n\t\t\t\t\tvec3 inscatter = GetSkyRadianceToPoint(\n\t\t\t\t\t\tcameraPosition,\n\t\t\t\t\t\tATMOSPHERE.bottom_radius * surfaceNormal,\n\t\t\t\t\t\tsunDirection,\n\t\t\t\t\t\ttransmittance\n\t\t\t\t\t);\n\t\t\t\t\tvec3 radiance = ATMOSPHERE.ground_albedo * RECIPROCAL_PI * (sunIrradiance + skyIrradiance);\n\t\t\t\t\toutputColor.rgb = radiance * transmittance + inscatter;\n\t\t\t\t\ttransmittance = vec3(0.0);\n\t\t\t\t} else {\n\t\t\t\t\toutputColor.rgb = GetSkyRadiance(\n\t\t\t\t\t\tcameraPosition,\n\t\t\t\t\t\trayDirection,\n\t\t\t\t\t\tsunDirection,\n\t\t\t\t\t\ttransmittance\n\t\t\t\t\t);\n\t\t\t\t}\n\t\t\t#else\n\t\t\t\toutputColor.rgb = GetSkyRadiance(\n\t\t\t\t\tcameraPosition,\n\t\t\t\t\trayDirection,\n\t\t\t\t\tsunDirection,\n\t\t\t\t\ttransmittance\n\t\t\t\t);\n\t\t\t#endif\n\n\t\t\toutputColor.rgb = ToneMapping(outputColor.rgb);\n\t\t\t\n\t\t\t\t\t\t#ifdef SKY_SUNDISK\n\t\t\t\tfloat nu = dot(rayDirection, sunDirection);\n\t\t\t\tfloat sun = 0.004 * sunDiskSize * MiePhaseFunction(0.99, nu);\n\t\t\t\t\t\toutputColor.rgb += sun * transmittance;\n\t\t\t\t\t\t#endif\n\n\t\t\toutputColor.a = 1.0;\n\n\t\t\t\t\t\tgl_FragColor = outputColor;\n\n\t\t\t#ifdef SRGB_OUTPUT\n\t\t\t\tgl_FragColor = LinearTosRGB(gl_FragColor);\n\t\t\t#endif\n\n\t\t\t#include <dithering_frag>\n\t\t\t\t}\n\t\t`},d=new e.Vector3,p=new e.Vector3;function l(t,e,n,r){const a=n.getPositionToSurfacePoint(t,d);return null!=a?function(t,e,n,r){const a=t.radius.x**2,o=t.radius.z**2,i=p.set(e.x/a,e.y/a,e.z/o).normalize();return r.copy(i.multiplyScalar(-n).add(e))}(n,a,e,r).negate():r.setScalar(0)}const h=new e.Vector3,g=new e.Vector3;class T extends e.Mesh{constructor(){const t=new e.ShaderMaterial(_);t.depthWrite=!1,t.side=e.DRAW_SIDE.BACK,t.dithering=!0,super(new e.PlaneGeometry(2,2),t),this.frustumCulled=!1}setLUTs(t){const{atmosphere:e,transmittanceTexture:n,inscatterTexture:r,irradianceTexture:a}=t,{uniforms:o,defines:i}=this.material;o.ATMOSPHERE=e.toUniform(),e.sunRadianceToRelativeLuminance.toArray(o.SUN_SPECTRAL_RADIANCE_TO_LUMINANCE),e.skyRadianceToRelativeLuminance.toArray(o.SKY_SPECTRAL_RADIANCE_TO_LUMINANCE),o.transmittance_texture=n,o.scattering_texture=r,o.irradiance_texture=a;let s=!1;i.TRANSMITTANCE_MAPPING!==t.transmittanceMapping&&(i.TRANSMITTANCE_MAPPING=t.transmittanceMapping,s=!0),i.INSCATTER_MAPPING!==t.inscatterMapping&&(i.INSCATTER_MAPPING=t.inscatterMapping,s=!0),this.material.needsUpdate=s}setCamera(t,e,n,r=c.DEFAULT){const{uniforms:a}=this.material;if(h.setFromMatrixPosition(t.worldMatrix),h.toArray(a.cameraPosition),e.toArray(a.worldToECEFMatrix),n){const t=n.ellipsoid;if(void 0===n.correctAltitude||n.correctAltitude){l(h.applyMatrix4(e),r.bottomRadius,t,g).toArray(a.altitudeCorrection)}else g.set(0,0,0).toArray(a.altitudeCorrection)}else g.set(0,0,0).toArray(a.altitudeCorrection)}}const f={name:"atmos_fog",defines:{TRANSMITTANCE_MAPPING:1,INSCATTER_MAPPING:1,CORRECT_GEOMETRIC_ERROR:!0,SUN_LIGHT:!0,SKY_LIGHT:!0,TRANSMITTANCE:!0,INSCATTER:!0},uniforms:{ATMOSPHERE:c.DEFAULT.toUniform(),SUN_SPECTRAL_RADIANCE_TO_LUMINANCE:[0,0,0],SKY_SPECTRAL_RADIANCE_TO_LUMINANCE:[0,0,0],scattering_texture:null,transmittance_texture:null,irradiance_texture:null,cameraPosition:[0,0,0],sunDirection:[0,0,0],worldToECEFMatrix:new Array(16),altitudeCorrection:[0,0,0],toneMappingExposure:10,tDiffuse:null,depthTexture:null,normalTexture:null,projectionView:new Array(16),anchorMatrix:new Array(16),ellipsoidRadii:new Array(3),geometricErrorCorrectionAmount:0,albedoScale:2/Math.PI},vertexShader:`\n\t\t#define METER_TO_LENGTH_UNIT ${i.toFixed(7)}\n\n\t\tattribute vec3 a_Position;\n\t\tattribute vec2 a_Uv;\n\n\t\tuniform mat4 u_ProjectionView;\n\t\tuniform mat4 u_Model;\n\n\t\tuniform vec3 cameraPosition;\n\t\tuniform mat4 worldToECEFMatrix;\n\t\tuniform vec3 altitudeCorrection;\n\t\tuniform vec3 ellipsoidRadii;\n\t\tuniform float geometricErrorCorrectionAmount;\n\n\t\tvarying vec3 vCameraPosition;\n\t\tvarying vec3 vEllipsoidRadiiSquared;\n\t\tvarying vec3 vGeometryAltitudeCorrection;\n\t\tvarying vec2 v_Uv;\n\n\t\tvoid main() {\n\t\t\tgl_Position = u_ProjectionView * u_Model * vec4(a_Position, 1.0);\n\n\t\t\tvec3 cameraPositionECEF = (worldToECEFMatrix * vec4(cameraPosition, 1.0)).xyz;\n\t\t\tvCameraPosition = (cameraPositionECEF + altitudeCorrection) * METER_TO_LENGTH_UNIT;\n\n\t\t\tvGeometryAltitudeCorrection = altitudeCorrection * METER_TO_LENGTH_UNIT;\n\t\t\t#ifdef CORRECT_GEOMETRIC_ERROR\n\t\t\t\tvGeometryAltitudeCorrection *= 1.0 - geometricErrorCorrectionAmount;\n\t\t\t#endif\n\n\t\t\tvec3 radii = ellipsoidRadii * METER_TO_LENGTH_UNIT;\n\t\t\t\tvEllipsoidRadiiSquared = radii * radii;\n\n\t\t\tv_Uv = a_Uv;\n\t\t}\n\t`,fragmentShader:`\n\t\t${n.octahedronToUnitVectorGLSL}\n\n\t\t${s}\n\t\t${r}\n\t\t${a}\n\n\t\tuniform AtmosphereParameters ATMOSPHERE;\n\t\tuniform vec3 SUN_SPECTRAL_RADIANCE_TO_LUMINANCE;\n\t\tuniform vec3 SKY_SPECTRAL_RADIANCE_TO_LUMINANCE;\n\n\t\tuniform highp sampler3D scattering_texture;\n\t\t\t\tuniform sampler2D transmittance_texture;\n\t\tuniform sampler2D irradiance_texture;\n\n\t\t${o}\n\n\t\tuniform float toneMappingExposure;\n\n\t\tuniform vec3 sunDirection;\n\n\t\tuniform sampler2D tDiffuse;\n\t\tuniform sampler2D depthTexture;\n\t\tuniform sampler2D normalTexture;\n\t\tuniform mat4 projectionView;\n\t\tuniform mat4 anchorMatrix;\n\t\tuniform mat4 worldToECEFMatrix;\n\t\tuniform float geometricErrorCorrectionAmount;\n\t\tuniform float albedoScale;\n\n\t\tvarying vec3 vCameraPosition;\n\t\tvarying vec3 vEllipsoidRadiiSquared;\n\t\tvarying vec3 vGeometryAltitudeCorrection;\n\t\tvarying vec2 v_Uv;\n\n\t\tvoid correctGeometricError(inout vec3 positionECEF, inout vec3 normalECEF) {\n\t\t\t// TODO: The error is pronounced at the edge of the ellipsoid due to the\n\t\t\t// large difference between the sphere position and the unprojected position\n\t\t\t// at the current fragment. Calculating the sphere position from the fragment\n\t\t\t// UV may resolve this.\n\n\t\t\t// Correct way is slerp, but this will be small-angle interpolation anyways.\n\t\t\tvec3 sphereNormal = normalize(positionECEF / vEllipsoidRadiiSquared);\n\t\t\tvec3 spherePosition = ATMOSPHERE.bottom_radius * sphereNormal;\n\t\t\tnormalECEF = mix(normalECEF, sphereNormal, geometricErrorCorrectionAmount);\n\t\t\tpositionECEF = mix(positionECEF, spherePosition, geometricErrorCorrectionAmount);\n\t\t}\n\n\t\t\t\tvoid main() {\n\t\t\tvec2 uv = v_Uv;\n\n\t\t\tvec4 inputColor = texture2D(tDiffuse, uv);\n\n\t\t\tfloat depth = texture2D(depthTexture, uv).r;\n\t\t\tvec4 gBufferTexel = texture2D(normalTexture, uv);\n\n\t\t\t// if (depth >= 1.0 - 1e-8) {\n\t\t\t// \tgl_FragColor = inputColor;\n\t\t\t// \treturn;\n\t\t\t// }\n\n\t\t\tif (gBufferTexel.r < -2.0) {\n\t\t\t\tgl_FragColor = inputColor;\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tvec4 clipPosition = vec4(vec3(uv, depth) * 2.0 - 1.0, 1.0);\n\t\t\tvec4 worldPosition4 = anchorMatrix * (inverse(projectionView) * clipPosition);\n\n\t\t\tvec3 worldPosition = worldPosition4.xyz / worldPosition4.w;\n\t\t\tvec3 worldNormal = octahedronToUnitVector(gBufferTexel.rg);\n\t\t\tworldNormal = (anchorMatrix * vec4(worldNormal, 0.0)).xyz;\n\t\t\tworldNormal = normalize(worldNormal);\n\n\t\t\tvec3 positionECEF = (worldToECEFMatrix * vec4(worldPosition, 1.0)).xyz;\n\t\t\tpositionECEF = positionECEF * METER_TO_LENGTH_UNIT + vGeometryAltitudeCorrection;\n\t\t\tvec3 normalECEF = (worldToECEFMatrix * vec4(worldNormal, 0.0)).xyz;\n\n\t\t\t#ifdef CORRECT_GEOMETRIC_ERROR\n\t\t\t\tcorrectGeometricError(positionECEF, normalECEF);\n\t\t\t#endif\n\n\t\t\tvec3 radiance;\n\t\t\t#if defined(SUN_LIGHT) || defined(SKY_LIGHT)\n\t\t\t\tvec3 diffuse = inputColor.rgb * albedoScale * RECIPROCAL_PI;\n\t\t\t\tvec3 skyIrradiance;\n\t\t\t\t\tvec3 sunIrradiance = GetSunAndSkyIrradiance(\n\t\t\t\t\tpositionECEF,\n\t\t\t\t\tnormalECEF,\n\t\t\t\t\tsunDirection,\n\t\t\t\t\tskyIrradiance\n\t\t\t\t);\n\n\t\t\t\t#if defined(SUN_LIGHT) && defined(SKY_LIGHT)\n\t\t\t\t\tradiance = diffuse * (sunIrradiance + skyIrradiance);\n\t\t\t\t#elif defined(SUN_LIGHT)\n\t\t\t\t\tradiance = diffuse * sunIrradiance;\n\t\t\t\t#elif defined(SKY_LIGHT)\n\t\t\t\t\tradiance = diffuse * skyIrradiance;\n\t\t\t\t#endif\n\t\t\t#else\n\t\t\t\tradiance = inputColor.rgb;\n\t\t\t#endif\n\n\t\t\t#if defined(TRANSMITTANCE) || defined(INSCATTER)\n\t\t\t\tvec3 transmittance;\n\t\t\t\tvec3 inscatter = GetSkyRadianceToPoint(\n\t\t\t\t\tvCameraPosition,\n\t\t\t\t\tpositionECEF,\n\t\t\t\t\tsunDirection,\n\t\t\t\t\ttransmittance\n\t\t\t\t);\n\n\t\t\t\t#ifdef TRANSMITTANCE\n\t\t\t\t\tradiance *= transmittance;\n\t\t\t\t#endif\n\t\t\t\t#ifdef INSCATTER\n\t\t\t\t\tradiance += inscatter;\n\t\t\t\t#endif\n\t\t\t#endif\n\t\t\t\n\t\t\t\t\t\tgl_FragColor = vec4(radiance, inputColor.a);\n\t\t\t\t}\n\t`},E=new e.Vector3,v=new e.Vector3,x={};class A extends n.Effect{constructor(){super(),this.bufferDependencies=[{key:"GBuffer"}],this._mainPass=new e.ShaderPostPass(f)}setLUTs(t){const{atmosphere:e,transmittanceTexture:n,inscatterTexture:r,irradianceTexture:a}=t,{uniforms:o,defines:i}=this._mainPass.material;o.ATMOSPHERE=e.toUniform(),e.sunRadianceToRelativeLuminance.toArray(o.SUN_SPECTRAL_RADIANCE_TO_LUMINANCE),e.skyRadianceToRelativeLuminance.toArray(o.SKY_SPECTRAL_RADIANCE_TO_LUMINANCE),o.transmittance_texture=n,o.scattering_texture=r,o.irradiance_texture=a;let s=!1;i.TRANSMITTANCE_MAPPING!==t.transmittanceMapping&&(i.TRANSMITTANCE_MAPPING=t.transmittanceMapping,s=!0),i.INSCATTER_MAPPING!==t.inscatterMapping&&(i.INSCATTER_MAPPING=t.inscatterMapping,s=!0),this._mainPass.material.needsUpdate=s}setCamera(t,n,r,a=c.DEFAULT){const{uniforms:o}=this._mainPass.material;if(E.setFromMatrixPosition(t.worldMatrix),E.toArray(o.cameraPosition),n.toArray(o.worldToECEFMatrix),r){const i=r.ellipsoid,s=void 0===r.correctAltitude||r.correctAltitude,m=E.applyMatrix4(n);s?l(m,a.bottomRadius,i,v).toArray(o.altitudeCorrection):v.set(0,0,0).toArray(o.altitudeCorrection),i.radius.toArray(o.ellipsoidRadii);const u=i.getPositionToCartographic(m,x).height,c=v.set(0,Math.max(...i.radius),-Math.max(0,u)).applyMatrix4(t.projectionMatrix),_=e.MathUtils.mapLinear(c.y,41.5,13.8,0,1);o.geometricErrorCorrectionAmount=e.MathUtils.clamp(_,0,1)}else v.set(0,0,0),v.toArray(o.altitudeCorrection),v.toArray(o.ellipsoidRadii),o.geometricErrorCorrectionAmount=1}render(t,n,r,a,o){const i=n.getBuffer("GBuffer"),s=i.getCurrentRenderStates();s.camera.projectionViewMatrix.toArray(this._mainPass.uniforms.projectionView),s.scene.anchorMatrix.toArray(this._mainPass.uniforms.anchorMatrix),t.setRenderTarget(a),t.setClearColor(0,0,0,0),o?t.clear(n.clearColor,n.clearDepth,n.clearStencil):t.clear(!0,!0,!1);const m=this._mainPass;m.uniforms.tDiffuse=r.texture,m.uniforms.depthTexture=i.output()._attachments[e.ATTACHMENT.DEPTH_STENCIL_ATTACHMENT],m.uniforms.normalTexture=i.output()._attachments[e.ATTACHMENT.COLOR_ATTACHMENT0],o&&(m.material.transparent=n._tempClearColor[3]<1||!n.clearColor,m.renderStates.camera.rect.fromArray(n._tempViewport)),m.render(t),o&&(m.material.transparent=!1,m.renderStates.camera.rect.set(0,0,1,1))}dispose(){this._mainPass.dispose()}}const S="\nNumber GetLayerDensity(const DensityProfileLayer layer, const Length altitude) {\n\tNumber density = layer.exp_term * exp(layer.exp_scale * altitude) +\n\tlayer.linear_term * altitude + layer.constant_term;\n\treturn clamp(density, Number(0.0), Number(1.0));\n}\n\nNumber GetProfileDensity(const DensityProfile profile, const Length altitude) {\n\tDensityProfileLayer layers[2] = profile.layers;\n\treturn altitude < layers[0].width\n\t\t? GetLayerDensity(layers[0], altitude)\n\t\t: GetLayerDensity(layers[1], altitude);\n}\n\nLength ComputeOpticalLengthToTopAtmosphereBoundary(\n\tconst AtmosphereParameters atmosphere, const DensityProfile profile,\n\tconst Length r, const Number mu) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\t// Number of intervals for the numerical integration.\n\tconst int SAMPLE_COUNT = 500;\n\t// The integration step, i.e. the length of each integration interval.\n\tLength dx =\n\t\tDistanceToTopAtmosphereBoundary(atmosphere, r, mu) / Number(SAMPLE_COUNT);\n\t// Integration loop.\n\tLength result = 0.0 * m;\n\tfor (int i = 0; i <= SAMPLE_COUNT; ++i) {\n\t\tLength d_i = Number(i) * dx;\n\t\t// Distance between the current sample point and the planet center.\n\t\tLength r_i = sqrt(d_i * d_i + 2.0 * r * mu * d_i + r * r);\n\t\t// Number density at the current sample point (divided by the number density\n\t\t// at the bottom of the atmosphere, yielding a dimensionless number).\n\t\tNumber y_i = GetProfileDensity(profile, r_i - atmosphere.bottom_radius);\n\t\t// Sample weight (from the trapezoidal rule).\n\t\tNumber weight_i = i == 0 || i == SAMPLE_COUNT ? 0.5 : 1.0;\n\t\tresult += y_i * weight_i * dx;\n\t}\n\treturn result;\n}\n\nDimensionlessSpectrum ComputeTransmittanceToTopAtmosphereBoundary(\n\tconst AtmosphereParameters atmosphere, const Length r, const Number mu) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tvec3 optical_depth = \n\t\tatmosphere.rayleigh_scattering *\n\t\t\tComputeOpticalLengthToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, atmosphere.rayleigh_density, r, mu) +\n\t\tatmosphere.mie_extinction *\n\t\t\tComputeOpticalLengthToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, atmosphere.mie_density, r, mu);\n\n\t#if TRANSMITTANCE_MAPPING == 2\n\t\toptical_depth += atmosphere.absorption_extinction *\n\t\t\tComputeOpticalLengthToTopAtmosphereBoundary(\n\t\t\t\tatmosphere, atmosphere.absorption_density, r, mu);\n\t#endif\n\treturn exp(-optical_depth);\n}\n\nNumber GetUnitRangeFromTextureCoord(const Number u, const int texture_size) {\n\treturn (u - 0.5 / Number(texture_size)) / (1.0 - 1.0 / Number(texture_size));\n}\n\n#if TRANSMITTANCE_MAPPING == 0\n\tvoid GetRMuFromTransmittanceTextureUv(const AtmosphereParameters atmosphere,\n\t\tconst vec2 uv, out Length r, out Number mu) {\n\t\tassert(uv.x >= 0.0 && uv.x <= 1.0);\n\t\t\tassert(uv.y >= 0.0 && uv.y <= 1.0);\n\t\tmu = -0.15 + uv.x * (1.0 + 0.15);\n\t\tr = atmosphere.bottom_radius + uv.y * (atmosphere.top_radius - atmosphere.bottom_radius);\n\t}\n#elif TRANSMITTANCE_MAPPING == 1\n\tvoid GetRMuFromTransmittanceTextureUv(const AtmosphereParameters atmosphere,\n\t\tconst vec2 uv, out Length r, out Number mu) {\n\t\tassert(uv.x >= 0.0 && uv.x <= 1.0);\n\t\t\tassert(uv.y >= 0.0 && uv.y <= 1.0);\n\t\tmu = -0.15 + tan(1.5 * uv.x) / tan(1.5) * (1.0 + 0.15);\n\t\tr = atmosphere.bottom_radius + (uv.y * uv.y) * (atmosphere.top_radius - atmosphere.bottom_radius);\n\t}\n#else\n\tvoid GetRMuFromTransmittanceTextureUv(const AtmosphereParameters atmosphere,\n\t\tconst vec2 uv, out Length r, out Number mu) {\n\t\tassert(uv.x >= 0.0 && uv.x <= 1.0);\n\t\t\tassert(uv.y >= 0.0 && uv.y <= 1.0);\n\t\tNumber x_mu = GetUnitRangeFromTextureCoord(uv.x, TRANSMITTANCE_TEXTURE_WIDTH);\n\t\tNumber x_r = GetUnitRangeFromTextureCoord(uv.y, TRANSMITTANCE_TEXTURE_HEIGHT);\n\t\t// Distance to top atmosphere boundary for a horizontal ray at ground level.\n\t\tLength H = sqrt(atmosphere.top_radius * atmosphere.top_radius -\n\t\t\tatmosphere.bottom_radius * atmosphere.bottom_radius);\n\t\t// Distance to the horizon, from which we can compute r:\n\t\tLength rho = H * x_r;\n\t\tr = sqrt(rho * rho + atmosphere.bottom_radius * atmosphere.bottom_radius);\n\t\t// Distance to the top atmosphere boundary for the ray (r,mu), and its minimum\n\t\t// and maximum values over all mu - obtained for (r,1) and (r,mu_horizon) -\n\t\t// from which we can recover mu:\n\t\tLength d_min = atmosphere.top_radius - r;\n\t\tLength d_max = rho + H;\n\t\tLength d = d_min + x_mu * (d_max - d_min);\n\t\tmu = d <= 0.0 ? Number(1.0) : (H * H - rho * rho - d * d) / (2.0 * r * d);\n\t\tmu = ClampCosine(mu);\n\t}\n#endif\n\nDimensionlessSpectrum ComputeTransmittanceToTopAtmosphereBoundaryTexture(\n\tconst AtmosphereParameters atmosphere, const vec2 frag_coord) {\n\tconst vec2 TRANSMITTANCE_TEXTURE_SIZE =\n\t\tvec2(TRANSMITTANCE_TEXTURE_WIDTH, TRANSMITTANCE_TEXTURE_HEIGHT);\n\tLength r;\n\tNumber mu;\n\tGetRMuFromTransmittanceTextureUv(\n\t\tatmosphere, frag_coord / TRANSMITTANCE_TEXTURE_SIZE, r, mu);\n\treturn ComputeTransmittanceToTopAtmosphereBoundary(atmosphere, r, mu);\n}\n\nvoid ComputeSingleScatteringIntegrand(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu, \n\tconst Length d, const bool ray_r_mu_intersects_ground, \n\tout DimensionlessSpectrum rayleigh, out DimensionlessSpectrum mie) {\n\tLength r_d = ClampRadius(atmosphere, sqrt(d * d + 2.0 * r * mu * d + r * r));\n\tNumber mu_s_d = ClampCosine((r * mu_s + d * nu) / r_d);\n\tDimensionlessSpectrum transmittance = \n\t\tGetTransmittance(\n\t\t\tatmosphere, transmittance_texture, r, mu, d, \n\t\t\tray_r_mu_intersects_ground) *\n\t\tGetTransmittanceToSun(\n\t\t\tatmosphere, transmittance_texture, r_d, mu_s_d);\n\trayleigh = exp(-(r_d - atmosphere.bottom_radius) / HR) * transmittance;\n\tmie = DimensionlessSpectrum(exp(-(r_d - atmosphere.bottom_radius) / HM) * transmittance.r); // only calc the red channel\n}\n\nfloat DistanceToNearestAtmosphereBoundary(const AtmosphereParameters atmosphere,\n\tLength r, Number mu, bool ray_r_mu_intersects_ground) {\n\tif (ray_r_mu_intersects_ground) {\n\t\treturn DistanceToBottomAtmosphereBoundary(atmosphere, r, mu);\n\t} else {\n\t\treturn DistanceToTopAtmosphereBoundary(atmosphere, r, mu);\n\t}\n}\n\nvoid ComputeSingleScattering(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu, \n\tconst bool ray_r_mu_intersects_ground, \n\tout DimensionlessSpectrum rayleigh, out DimensionlessSpectrum mie) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu >= -1.0 && mu <= 1.0);\n\tassert(mu_s >= -1.0 && mu_s <= 1.0);\n\tassert(nu >= -1.0 && nu <= 1.0);\n\n\t// Number of intervals for the numerical integration.\n\t\tconst int SAMPLE_COUNT = 25;\n\t// The integration step, i.e. the length of each integration interval.\n\tLength dx = DistanceToNearestAtmosphereBoundary(atmosphere, r, mu, \n\t\tray_r_mu_intersects_ground) / Number(SAMPLE_COUNT);\n\t// Integration loop.\n\tDimensionlessSpectrum rayleigh_sum = DimensionlessSpectrum(0.0);\n\tDimensionlessSpectrum mie_sum = DimensionlessSpectrum(0.0);\n\tfor (int i = 0; i <= SAMPLE_COUNT; ++i) {\n\t\tLength d_i = Number(i) * dx;\n\t\t// The Rayleigh and Mie single scattering at the current sample point.\n\t\tDimensionlessSpectrum rayleigh_i;\n\t\tDimensionlessSpectrum mie_i;\n\t\tComputeSingleScatteringIntegrand(atmosphere, transmittance_texture,\n\t\t\tr, mu, mu_s, nu, d_i, ray_r_mu_intersects_ground, rayleigh_i, mie_i);\n\t\t// Sample weight (from the trapezoidal rule).\n\t\t\tNumber weight_i = (i == 0 || i == SAMPLE_COUNT) ? 0.5 : 1.0;\n\t\trayleigh_sum += rayleigh_i * weight_i;\n\t\t\tmie_sum += mie_i * weight_i;\n\t}\n\trayleigh = rayleigh_sum * dx * atmosphere.solar_irradiance * \n\t\tatmosphere.rayleigh_scattering;\n\tmie = mie_sum * dx * atmosphere.solar_irradiance * atmosphere.mie_scattering;\n}\n\nvoid GetRMuMuSNuFromScatteringTextureUvwz(const AtmosphereParameters atmosphere,\n\tvec4 uvwz, out Length r, out Number mu, out Number mu_s,\n\tout Number nu, out bool ray_r_mu_intersects_ground) {\n\tassert(uvwz.x >= 0.0 && uvwz.x <= 1.0);\n\tassert(uvwz.y >= 0.0 && uvwz.y <= 1.0);\n\tassert(uvwz.z >= 0.0 && uvwz.z <= 1.0);\n\tassert(uvwz.w >= 0.0 && uvwz.w <= 1.0);\n\n\t// Distance to top atmosphere boundary for a horizontal ray at ground level.\n\tLength H = sqrt(atmosphere.top_radius * atmosphere.top_radius -\n\t\tatmosphere.bottom_radius * atmosphere.bottom_radius);\n\t// Distance to the horizon.\n\tLength rho =\n\t\tH * GetUnitRangeFromTextureCoord(uvwz.w, SCATTERING_TEXTURE_R_SIZE);\n\tr = sqrt(rho * rho + atmosphere.bottom_radius * atmosphere.bottom_radius);\n\n\t#if INSCATTER_MAPPING == 1\n\t\tif (uvwz.z < 0.5) {\n\t\t\t// Distance to the ground for the ray (r,mu), and its minimum and maximum\n\t\t\t// values over all mu - obtained for (r,-1) and (r,mu_horizon) - from which\n\t\t\t// we can recover mu:\n\t\t\tLength d_min = r - atmosphere.bottom_radius;\n\t\t\tLength d_max = rho;\n\t\t\tLength d = d_min + (d_max - d_min) * GetUnitRangeFromTextureCoord(\n\t\t\t\t1.0 - 2.0 * uvwz.z, SCATTERING_TEXTURE_MU_SIZE / 2);\n\t\t\tmu = d == 0.0 * m ? Number(-1.0) : \n\t\t\t\tClampCosine(-(rho * rho + d * d) / (2.0 * r * d));\n\t\t\tray_r_mu_intersects_ground = true;\n\t\t} else {\n\t\t\t// Distance to the top atmosphere boundary for the ray (r,mu), and its\n\t\t\t// minimum and maximum values over all mu - obtained for (r,1) and\n\t\t\t// (r,mu_horizon) - from which we can recover mu:\n\t\t\tLength d_min = atmosphere.top_radius - r;\n\t\t\tLength d_max = rho + H;\n\t\t\tLength d = d_min + (d_max - d_min) * GetUnitRangeFromTextureCoord(\n\t\t\t\t2.0 * uvwz.z - 1.0, SCATTERING_TEXTURE_MU_SIZE / 2);\n\t\t\tmu = d == 0.0 * m ? Number(1.0) :\n\t\t\t\tClampCosine((H * H - rho * rho - d * d) / (2.0 * r * d));\n\t\t\tray_r_mu_intersects_ground = false;\n\t\t}\n\n\t\tNumber x_mu_s =\n\t\t\tGetUnitRangeFromTextureCoord(uvwz.y, SCATTERING_TEXTURE_MU_S_SIZE);\n\t\tLength d_min = atmosphere.top_radius - atmosphere.bottom_radius;\n\t\tLength d_max = H;\n\t\tLength D = DistanceToTopAtmosphereBoundary(\n\t\t\tatmosphere, atmosphere.bottom_radius, atmosphere.mu_s_min);\n\t\tNumber A = (D - d_min) / (d_max - d_min);\n\t\tNumber a = (A - x_mu_s * A) / (1.0 + x_mu_s * A);\n\t\tLength d = d_min + min(a, A) * (d_max - d_min);\n\t\tmu_s = d == 0.0 * m ? Number(1.0) :\n\t\t\tClampCosine((H * H - d * d) / (2.0 * atmosphere.bottom_radius * d));\n\t#else \n\t\tNumber x_mu_s = GetUnitRangeFromTextureCoord(uvwz.y, SCATTERING_TEXTURE_MU_S_SIZE);\n\t\tmu = -1.0 + 2.0 * GetUnitRangeFromTextureCoord(uvwz.z, SCATTERING_TEXTURE_MU_SIZE);\n\t\tmu_s = -0.2 + x_mu_s * 1.2;\n\t#endif\n\n\tnu = ClampCosine(uvwz.x * 2.0 - 1.0);\n}\n\nvoid GetRMuMuSNuFromScatteringTextureFragCoord(\n\tconst AtmosphereParameters atmosphere, const vec3 frag_coord,\n\tout Length r, out Number mu, out Number mu_s, out Number nu,\n\tout bool ray_r_mu_intersects_ground) {\n\tconst vec4 SCATTERING_TEXTURE_SIZE = vec4(\n\t\tSCATTERING_TEXTURE_NU_SIZE - 1,\n\t\tSCATTERING_TEXTURE_MU_S_SIZE,\n\t\tSCATTERING_TEXTURE_MU_SIZE,\n\t\tSCATTERING_TEXTURE_R_SIZE);\n\tNumber frag_coord_nu =\n\t\tfloor(frag_coord.x / Number(SCATTERING_TEXTURE_MU_S_SIZE));\n\tNumber frag_coord_mu_s =\n\t\tmod(frag_coord.x, Number(SCATTERING_TEXTURE_MU_S_SIZE));\n\tvec4 uvwz =\n\t\tvec4(frag_coord_nu, frag_coord_mu_s, frag_coord.y, frag_coord.z) /\n\t\t\tSCATTERING_TEXTURE_SIZE;\n\tGetRMuMuSNuFromScatteringTextureUvwz(\n\t\tatmosphere, uvwz, r, mu, mu_s, nu, ray_r_mu_intersects_ground);\n\t// Clamp nu to its valid range of values, given mu and mu_s.\n\tnu = clamp(nu, mu * mu_s - sqrt((1.0 - mu * mu) * (1.0 - mu_s * mu_s)),\n\t\tmu * mu_s + sqrt((1.0 - mu * mu) * (1.0 - mu_s * mu_s)));\n}\n\nvoid ComputeSingleScatteringTexture(const AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture, const vec3 frag_coord,\n\tout IrradianceSpectrum rayleigh, out IrradianceSpectrum mie) {\n\tLength r;\n\tNumber mu;\n\tNumber mu_s;\n\tNumber nu;\n\tbool ray_r_mu_intersects_ground;\n\tGetRMuMuSNuFromScatteringTextureFragCoord(atmosphere, frag_coord,\n\t\tr, mu, mu_s, nu, ray_r_mu_intersects_ground);\n\tComputeSingleScattering(atmosphere, transmittance_texture,\n\t\tr, mu, mu_s, nu, ray_r_mu_intersects_ground, rayleigh, mie);\n}\n\nvec3 GetExtrapolatedSingleMieScattering(\n\tconst AtmosphereParameters atmosphere, const vec4 scattering) {\n\t// Algebraically this can never be negative, but rounding errors can produce\n\t// that effect for sufficiently short view rays.\n\t// @shotamatsuda: Avoid division by infinitesimal values.\n\t// See https://github.com/takram-design-engineering/three-geospatial/issues/47\n\tif (scattering.r < 1e-5) {\n\t\treturn vec3(0.0);\n\t}\n\treturn scattering.rgb * scattering.a / scattering.r *\n\t\t(atmosphere.rayleigh_scattering.r / atmosphere.mie_scattering.r) *\n\t\t(atmosphere.mie_scattering / atmosphere.rayleigh_scattering);\n}\n\nIrradianceSpectrum GetCombinedScattering(\n\tconst AtmosphereParameters atmosphere,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu, \n\tconst bool ray_r_mu_intersects_ground, out IrradianceSpectrum single_mie_scattering) {\n\tvec4 uvwz = GetScatteringTextureUvwzFromRMuMuSNu(\n\t\tatmosphere, r, mu, mu_s, nu, ray_r_mu_intersects_ground);\n\n\tNumber tex_coord_x = uvwz.x * Number(SCATTERING_TEXTURE_NU_SIZE - 1);\n\tNumber tex_x = floor(tex_coord_x);\n\tNumber lerp = tex_coord_x - tex_x;\n\tvec3 uvw0 = vec3((tex_x + uvwz.y) / Number(SCATTERING_TEXTURE_NU_SIZE),\n\t\tuvwz.z, uvwz.w);\n\tvec3 uvw1 = vec3((tex_x + 1.0 + uvwz.y) / Number(SCATTERING_TEXTURE_NU_SIZE),\n\t\tuvwz.z, uvwz.w);\n\n\tvec4 combined_scattering =\n\t\ttexture(scattering_texture, uvw0) * (1.0 - lerp) +\n\t\ttexture(scattering_texture, uvw1) * lerp;\n\tIrradianceSpectrum scattering = IrradianceSpectrum(combined_scattering);\n\tsingle_mie_scattering =\n\t\tGetExtrapolatedSingleMieScattering(atmosphere, combined_scattering);\n\n\treturn scattering;\n}\n\nRadianceSpectrum GetScattering(\n\tconst AtmosphereParameters atmosphere,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tconst Length r, const Number mu, const Number mu_s, const Number nu,\n\tconst bool ray_r_mu_intersects_ground) {\n\tIrradianceSpectrum mie;\n\tIrradianceSpectrum rayleigh = GetCombinedScattering(\n\t\tatmosphere, scattering_texture, r, mu, mu_s, nu,\n\t\tray_r_mu_intersects_ground, mie);\n\treturn rayleigh * RayleighPhaseFunction(nu) +\n\t\tmie * MiePhaseFunction(atmosphere.mie_phase_function_g, nu);\n}\n\nIrradianceSpectrum ComputeDirectIrradiance(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst Length r, const Number mu_s) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu_s >= -1.0 && mu_s <= 1.0);\n\n\tNumber alpha_s = atmosphere.sun_angular_radius / rad;\n\t// Approximate average of the cosine factor mu_s over the visible fraction of\n\t// the Sun disc.\n\tNumber average_cosine_factor =\n\t\tmu_s < -alpha_s ? 0.0 : (mu_s > alpha_s ? mu_s :\n\t\t(mu_s + alpha_s) * (mu_s + alpha_s) / (4.0 * alpha_s));\n\n\treturn atmosphere.solar_irradiance *\n\t\tGetTransmittanceToTopAtmosphereBoundary(\n\t\t\tatmosphere, transmittance_texture, r, mu_s) * average_cosine_factor;\n}\n\nIrradianceSpectrum ComputeIndirectIrradiance(\n\tconst AtmosphereParameters atmosphere,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tconst Length r, const Number mu_s) {\n\tassert(r >= atmosphere.bottom_radius && r <= atmosphere.top_radius);\n\tassert(mu_s >= -1.0 && mu_s <= 1.0);\n\tassert(scattering_order >= 1);\n\n\tconst int SAMPLE_COUNT = 32;\n\tconst Angle dphi = pi / Number(SAMPLE_COUNT);\n\tconst Angle dtheta = pi / Number(SAMPLE_COUNT);\n\n\tIrradianceSpectrum result =\n\t\tIrradianceSpectrum(0.0 * watt_per_square_meter_per_nm);\n\tvec3 omega_s = vec3(sqrt(1.0 - mu_s * mu_s), 0.0, mu_s);\n\tfor (int j = 0; j < SAMPLE_COUNT / 2; ++j) {\n\t\tAngle theta = (Number(j) + 0.5) * dtheta;\n\t\tfor (int i = 0; i < 2 * SAMPLE_COUNT; ++i) {\n\t\t\tAngle phi = (Number(i) + 0.5) * dphi;\n\t\t\tvec3 omega =\n\t\t\t\tvec3(cos(phi) * sin(theta), sin(phi) * sin(theta), cos(theta));\n\t\t\tSolidAngle domega = (dtheta / rad) * (dphi / rad) * sin(theta) * sr;\n\n\t\t\tNumber nu = dot(omega, omega_s);\n\t\t\tresult += GetScattering(atmosphere, scattering_texture,\n\t\t\t\tr, omega.z, mu_s, nu, false /* ray_r_theta_intersects_ground */) *\n\t\t\t\t\tomega.z * domega;\n\t\t}\n\t}\n\treturn result;\n}\n\nvoid GetRMuSFromIrradianceTextureUv(const AtmosphereParameters atmosphere,\n\tconst vec2 uv, out Length r, out Number mu_s) {\n\tassert(uv.x >= 0.0 && uv.x <= 1.0);\n\tassert(uv.y >= 0.0 && uv.y <= 1.0);\n\tNumber x_mu_s = GetUnitRangeFromTextureCoord(uv.x, IRRADIANCE_TEXTURE_WIDTH);\n\tNumber x_r = GetUnitRangeFromTextureCoord(uv.y, IRRADIANCE_TEXTURE_HEIGHT);\n\tr = atmosphere.bottom_radius +\n\t\tx_r * (atmosphere.top_radius - atmosphere.bottom_radius);\n\tmu_s = ClampCosine(2.0 * x_mu_s - 1.0);\n}\n\nconst vec2 IRRADIANCE_TEXTURE_SIZE =\n\tvec2(IRRADIANCE_TEXTURE_WIDTH, IRRADIANCE_TEXTURE_HEIGHT);\n\nIrradianceSpectrum ComputeDirectIrradianceTexture(\n\tconst AtmosphereParameters atmosphere,\n\tconst TransmittanceTexture transmittance_texture,\n\tconst vec2 frag_coord) {\n\tLength r;\n\tNumber mu_s;\n\tGetRMuSFromIrradianceTextureUv(\n\t\tatmosphere, frag_coord / IRRADIANCE_TEXTURE_SIZE, r, mu_s);\n\treturn ComputeDirectIrradiance(atmosphere, transmittance_texture, r, mu_s);\n}\n\nIrradianceSpectrum ComputeIndirectIrradianceTexture(\n\tconst AtmosphereParameters atmosphere,\n\tconst highp ReducedScatteringTexture scattering_texture,\n\tconst vec2 frag_coord) {\n\tLength r;\n\tNumber mu_s;\n\tGetRMuSFromIrradianceTextureUv(\n\t\tatmosphere, frag_coord / IRRADIANCE_TEXTURE_SIZE, r, mu_s);\n\treturn ComputeIndirectIrradiance(atmosphere,\n\t\tscattering_texture,\n\t\tr, mu_s);\n}\n",R={name:"atmos_transmittance",defines:{TRANSMITTANCE_MAPPING:1,INSCATTER_MAPPING:1},uniforms:{},vertexShader:"\n\t\t\t\tattribute vec3 a_Position;\n\t\t\t\tattribute vec2 a_Uv;\n\t\t\t\t\t \n\t\t\t\tuniform mat4 u_ProjectionView;\n\t\t\t\tuniform mat4 u_Model;\n\n\t\t\t\tvarying vec2 v_Uv;\n\n\t\t\t\tvoid main() {\n\t\t\t\t\t\tv_Uv = a_Uv;\n\t\t\t\t\t\tgl_Position = u_ProjectionView * u_Model * vec4(a_Position, 1.0);\n\t\t\t\t}\n\t\t",fragmentShader:`\n\t\t${s}\n\t\t${r}\n\t\t${a}\n\t\t${S}\n\n\t\tuniform AtmosphereParameters ATMOSPHERE;\n\n\t\tvarying vec2 v_Uv;\n\n\t\t\t\tvoid main() {\n\t\t\tvec4 transmittance;\n\t\t\ttransmittance.rgb = ComputeTransmittanceToTopAtmosphereBoundaryTexture(\n\t\t\t\tATMOSPHERE, gl_FragCoord.xy\n\t\t\t);\n\t\t\ttransmittance.a = 1.0;\n\t\t\t\t\t\tgl_FragColor = transmittance;\n\t\t\t\t}\n\t\t`},y={name:"atmos_inscatter",defines:{TRANSMITTANCE_MAPPING:1,INSCATTER_MAPPING:1},uniforms:{transmittance_texture:null,layer:0},vertexShader:"\n\t\t\t\tattribute vec3 a_Position;\n\t\t\t\tattribute vec2 a_Uv;\n\t\t\t\t\t \n\t\t\t\tuniform mat4 u_ProjectionView;\n\t\t\t\tuniform mat4 u_Model;\n\n\t\t\t\tvarying vec2 v_Uv;\n\n\t\t\t\tvoid main() {\n\t\t\t\t\t\tv_Uv = a_Uv;\n\t\t\t\t\t\tgl_Position = u_ProjectionView * u_Model * vec4(a_Position, 1.0);\n\t\t\t\t}\n\t\t",fragmentShader:`\n\t\t${s}\n\t\t${r}\n\t\t${a}\n\t\t${S}\n\n\t\tuniform AtmosphereParameters ATMOSPHERE;\n\t\tuniform sampler2D transmittance_texture;\n\t\tuniform float layer;\n\n\t\tvarying vec2 v_Uv;\n\t\t\t\t\n\t\t\t\tvoid main() {\n\t\t\tvec4 deltaRayleigh;\n\t\t\tvec4 deltaMie;\n\t\t\tvec4 scattering;\n\t\t\tvec4 singleMieScattering;\n\t\t\tComputeSingleScatteringTexture(\n\t\t\t\tATMOSPHERE,\n\t\t\t\ttransmittance_texture,\n\t\t\t\tvec3(gl_FragCoord.xy, float(layer) + 0.5),\n\t\t\t\tdeltaRayleigh.rgb,\n\t\t\t\t\tdeltaMie.rgb\n\t\t\t);\n\t\t\tdeltaRayleigh.a = 1.0;\n\t\t\t\tdeltaMie.a = 1.0;\n\n\t\t\tgl_FragColor = vec4(deltaRayleigh.rgb, deltaMie.r);\n\t\t\t\t}\n\t\t`},N={name:"atmos_irradiance",defines:{TRANSMITTANCE_MAPPING:1,INSCATTER_MAPPING:1},uniforms:{transmittance_texture:null,scattering_texture:null},vertexShader:"\n\t\t\t\tattribute vec3 a_Position;\n\t\t\t\tattribute vec2 a_Uv;\n\t\t\t\t\t \n\t\t\t\tuniform mat4 u_ProjectionView;\n\t\t\t\tuniform mat4 u_Model;\n\n\t\t\t\tvarying vec2 v_Uv;\n\n\t\t\t\tvoid main() {\n\t\t\t\t\t\tv_Uv = a_Uv;\n\t\t\t\t\t\tgl_Position = u_ProjectionView * u_Model * vec4(a_Position, 1.0);\n\t\t\t\t}\n\t\t",fragmentShader:`\n\t\t${s}\n\t\t${r}\n\t\t${a}\n\t\t${S}\n\n\t\tuniform AtmosphereParameters ATMOSPHERE;\n\t\tuniform sampler2D transmittance_texture;\n\t\tuniform highp sampler3D scattering_texture;\n\n\t\tvarying vec2 v_Uv;\n\n\t\t\t\tvoid main() {\n\t\t\tvec3 deltaIrradiance;\n\t\t\tdeltaIrradiance = ComputeIndirectIrradianceTexture(\n\t\t\t\tATMOSPHERE,\n\t\t\t\tscattering_texture,\n\t\t\t\tgl_FragCoord.xy\n\t\t\t);\n\t\t\tgl_FragColor = vec4(deltaIrradiance, 1.0);\n\t\t\t\t}\n\t\t`};function I(t,n){const{width:r,height:a,texture:o}=n,i=o.type===e.PIXEL_TYPE.HALF_FLOAT?new Uint16Array(r*a*4):new Float32Array(r*a*4);t.setRenderTarget(n),t.readRenderTargetPixels(0,0,r,a,i),o.userData.imageData=i}function b(t,n){const r=new Uint16Array(t),a=r.length;if(n===e.PIXEL_TYPE.FLOAT){const t=new Float32Array(a);for(let n=0;n<a;n++)t[n]=e.MathUtils.fromHalfFloat(r[n]);return t}if(n===e.PIXEL_TYPE.HALF_FLOAT)return r;{const t=new Uint8Array(a);for(let n=0;n<a;n++)t[n]=Math.round(255*e.MathUtils.fromHalfFloat(r[n]));return t}}const P=new e.Vector3,C=new e.Vector3;function M(t){return Math.sqrt(Math.max(t,0))}function L(t,e,n){const{topRadius:r}=t;return a=-e*n+M(e**2*(n**2-1)+r**2),Math.max(a,0);var a}function w(t,e){return.5/e+t*(1-1/e)}const D=new e.Vector3,U=new e.Vector3,G=new e.Vector3;function F(t,e,n){const r=4*e;return n.fromArray(t,r,!0)}function O(t,n,r){const a=function(t){return t.image.data?t.image.data:t.userData.imageData?t.userData.imageData:void 0}(t);if(null==a)return r.setScalar(0);const{width:o,height:i}=t.image,s=e.MathUtils.clamp(n.x,0,1)*(o-1),m=e.MathUtils.clamp(n.y,0,1)*(i-1),u=Math.floor(s),c=Math.floor(m),_=s-u,d=m-c,p=u%o,l=(p+1)%o,h=c%i,g=(h+1)%i,T=F(a,h*o+p,D),f=F(a,h*o+l,U),E=T.lerp(f,_),v=F(a,g*o+p,U),x=F(a,g*o+l,G),A=v.lerp(x,_);return r.copy(E.lerp(A,d))}const z=1/Math.sqrt(Math.PI),H=Math.sqrt(3)/(2*Math.sqrt(Math.PI)),X=new e.Vector3,k=new e.Vector3,B=new e.Vector2,q=new e.Matrix3;function V(t,e,n,r){const{topRadius:a,bottomRadius:o}=t,i=Math.sqrt(a**2-o**2),s=M(e**2-o**2),m=a-e,u=(L(t,e,n)-m)/(s+i-m),c=s/i;return r.set(w(u,256),w(c,64))}const Y=new e.Vector3,Z=new e.Vector3,j=new e.Vector2;t.AtmosFogEffect=A,t.AtmosLUTsGenerator=class{constructor(t,n={}){const r=t.version>1,a=void 0!==n.atmosphere?n.atmosphere:c.DEFAULT,o=void 0!==n.transmittanceMapping?n.transmittanceMapping:1,i=void 0!==n.inscatterMapping?n.inscatterMapping:1,s=/(iPad|iPhone|iPod)/g.test(navigator.userAgent);let m;r?m=t.getExtension("EXT_color_buffer_float")&&t.getExtension("OES_texture_float_linear")&&!s?e.PIXEL_TYPE.FLOAT:e.PIXEL_TYPE.HALF_FLOAT:t.getExtension("OES_texture_float")&&t.getExtension("OES_texture_float_linear")&&!s?m=e.PIXEL_TYPE.FLOAT:t.getExtension("OES_texture_half_float")&&t.getExtension("OES_texture_half_float_linear")?m=e.PIXEL_TYPE.HALF_FLOAT:(m=e.PIXEL_TYPE.UNSIGNED_BYTE,console.warn("Half float texture is not supported!"));const u=new e.RenderTarget2D(256,64);u.texture.minFilter=e.TEXTURE_FILTER.LINEAR,u.texture.magFilter=e.TEXTURE_FILTER.LINEAR,u.texture.type=m,u.texture.format=e.PIXEL_FORMAT.RGBA,u.texture.generateMipmaps=!1;const _=new e.RenderTarget3D(256,128,32);_.texture.minFilter=e.TEXTURE_FILTER.LINEAR,_.texture.magFilter=e.TEXTURE_FILTER.LINEAR,_.texture.type=m,_.texture.format=e.PIXEL_FORMAT.RGBA,_.texture.generateMipmaps=!1;const d=new e.RenderTarget2D(64,16);d.texture.minFilter=e.TEXTURE_FILTER.LINEAR,d.texture.magFilter=e.TEXTURE_FILTER.LINEAR,d.texture.type=m,d.texture.format=e.PIXEL_FORMAT.RGBA,d.texture.generateMipmaps=!1;const p=a.toUniform(),l=new e.ShaderPostPass(R);l.uniforms.ATMOSPHERE=p,l.material.defines.TRANSMITTANCE_MAPPING=o;const h=new e.ShaderPostPass(y);h.uniforms.transmittance_texture=u.texture,h.uniforms.ATMOSPHERE=p,h.material.defines.TRANSMITTANCE_MAPPING=o,h.material.defines.INSCATTER_MAPPING=i;const g=new e.ShaderPostPass(N);g.uniforms.transmittance_texture=u.texture,g.uniforms.scattering_texture=_.texture,g.uniforms.ATMOSPHERE=p,g.material.defines.TRANSMITTANCE_MAPPING=o,g.material.defines.INSCATTER_MAPPING=i,this._transmittanceRT=u,this._inscatterRT=_,this._irradianceRT=d,this._transmittancePass=l,this._inscatterPass=h,this._irradiancePass=g,this._data={transmittanceTexture:u.texture,inscatterTexture:_.texture,irradianceTexture:d.texture,atmosphere:a,transmittanceMapping:o,inscatterMapping:i}}get data(){return this._data}computeTransmittance(t){t.setRenderTarget(this._transmittanceRT),t.setClearColor(0,0,0,0),t.clear(!0,!0,!0),this._transmittancePass.render(t)}computeInscatter(t){const e=this._inscatterRT,n=this._inscatterPass;if(e.isRenderTarget3D)for(let r=0;r<32;r++)e.activeLayer=r,n.uniforms.layer=r,t.setRenderTarget(e),t.setClearColor(0,0,0,0),t.clear(!0,!0,!0),n.render(t);else t.setRenderTarget(e),t.setClearColor(0,0,0,0),t.clear(!0,!0,!0),n.render(t)}computeIrradiance(t){t.setRenderTarget(this._irradianceRT),t.setClearColor(0,0,0,0),t.clear(!0,!0,!0),this._irradiancePass.render(t)}readTransmittancePixels(t){I(t,this._transmittanceRT)}readIrradiancePixels(t){I(t,this._irradianceRT)}dispose(){this._transmittanceRT.dispose(),this._inscatterRT.dispose(),this._irradianceRT.dispose(),this._transmittancePass.dispose(),this._inscatterPass.dispose(),this._irradiancePass.dispose()}},t.AtmosLUTsLoader=class{constructor(t,n={}){this._fileLoader=new e.FileLoader(n.manager),this._fileLoader.setResponseType("arraybuffer"),this._data={transmittanceTexture:null,inscatterTexture:null,irradianceTexture:null,atmosphere:c.DEFAULT,transmittanceMapping:2,inscatterMapping:1};let r=e.PIXEL_TYPE.FLOAT;t.version>1?r=t.getExtension("EXT_color_buffer_float")&&t.getExtension("OES_texture_float_linear")?e.PIXEL_TYPE.FLOAT:e.PIXEL_TYPE.HALF_FLOAT:t.getExtension("OES_texture_float")&&t.getExtension("OES_texture_float_linear")?r=e.PIXEL_TYPE.FLOAT:t.getExtension("OES_texture_half_float")&&t.getExtension("OES_texture_half_float_linear")?r=e.PIXEL_TYPE.HALF_FLOAT:(r=e.PIXEL_TYPE.UNSIGNED_BYTE,console.warn("Half float texture is not supported!"));const a=new e.Texture2D;a.minFilter=e.TEXTURE_FILTER.LINEAR,a.magFilter=e.TEXTURE_FILTER.LINEAR,a.type=r,a.generateMipmaps=!1,a.flipY=!1,this._data.transmittanceTexture=a;const o=new e.Texture3D;o.minFilter=e.TEXTURE_FILTER.LINEAR,o.magFilter=e.TEXTURE_FILTER.LINEAR,o.type=r,o.format=e.PIXEL_FORMAT.RGBA,o.generateMipmaps=!1,this._data.inscatterTexture=o;const i=new e.Texture2D;i.minFilter=e.TEXTURE_FILTER.LINEAR,i.magFilter=e.TEXTURE_FILTER.LINEAR,i.type=r,i.generateMipmaps=!1,i.flipY=!1,this._data.irradianceTexture=i}get data(){return this._data}loadTransmittanceTexture(t){return this._fileLoader.loadAsync(t).then(t=>{const e=this._data.transmittanceTexture;e.image={data:b(t,e.type),width:256,height:64},e.version++})}loadInscatterTexture(t){return this._fileLoader.loadAsync(t).then(t=>{const e=this._data.inscatterTexture;e.image={data:b(t,e.type),width:256,height:128,depth:32},e.version++})}loadIrradianceTexture(t){return this._fileLoader.loadAsync(t).then(t=>{const e=this._data.irradianceTexture;e.image={data:b(t,e.type),width:64,height:16},e.version++})}dispose(){this._data.transmittanceTexture.dispose(),this._data.inscatterTexture.dispose(),this._data.irradianceTexture.dispose()}},t.AtmosSky=T,t.getAltitudeCorrectionOffset=l,t.getScatteringCoefficient=function(t,n,r,a){const o=P.set(e.MathUtils.lerp(t.x+150,t.x-150,n.r),e.MathUtils.lerp(t.y+150,t.y-150,n.g),e.MathUtils.lerp(t.z+150,t.z-150,n.b));o.x=e.MathUtils.clamp(o.x,380,780),o.y=e.MathUtils.clamp(o.y,380,780),o.z=e.MathUtils.clamp(o.z,380,780);const i=o.multiplyScalar(1e-9),s=1.0003,m=C.set(Math.pow(i.x,4),Math.pow(i.y,4),Math.pow(i.z,4)).multiplyScalar(43939424999999996e10),u=8*Math.pow(Math.PI,3)*Math.pow(s*s-1,2)*6.105;return a.set(u/m.x,u/m.y,u/m.z),a.multiplyScalar(1e3*r),a},t.getSkyLightSH=function(t,n,r,a,o=new e.SphericalHarmonics3,i,s=c.DEFAULT){const m=q.setFromMatrix4(a).transpose(),u=X.copy(n).applyMatrix4(a);if(i){const t=i.ellipsoid;if(void 0===i.correctAltitude||i.correctAltitude){null!=t.getPositionToSurfacePoint(u,k)&&u.add(l(u,s.bottomRadius,t,k))}}const _=u.getLength(),d=function(t,e,n,r){const{topRadius:a,bottomRadius:o}=t,i=(e-o)/(a-o),s=.5*n+.5;return r.set(w(s,64),w(i,16))}(s,_,u.dot(r)/_,B),p=O(t,d,k);p.multiply(s.skyRadianceToRelativeLuminance);const h=X;i?i.ellipsoid.getPositionToNormal(u,h).applyMatrix3(m):h.set(0,1,0);const g=o.coefficients;g[0].copy(p).multiplyScalar(z),g[1].copy(p).multiplyScalar(H*h.y),g[2].copy(p).multiplyScalar(H*h.z),g[3].copy(p).multiplyScalar(H*h.x)},t.getSunLightColor=function(t,n,r,a,o=new e.Color3,i,s=c.DEFAULT){const m=Y.copy(n).applyMatrix4(a);if(i){const t=i.ellipsoid;if(void 0===i.correctAltitude||i.correctAltitude){null!=t.getPositionToSurfacePoint(m,Z)&&m.add(l(m,s.bottomRadius,t,Z))}}const u=m,_=Z;let d=u.getLength(),p=u.dot(r);const{topRadius:h}=s,g=-p-Math.sqrt(p**2-d**2+h**2);if(g>0&&(d=h,p+=g),d>h)_.set(1,1,1);else{const e=p/d,n=function(t,e,n){const{bottomRadius:r}=t;return n<0&&e**2*(n**2-1)+r**2>=0}(s,d,e);if(n)_.setScalar(0);else{O(t,V(s,d,e,j),_)}}const T=_.multiply(s.solarIrradiance).multiply(s.sunRadianceToRelativeLuminance);return o.setRGB(T.x,T.y,T.z)}});
